# Production readiness review

This record separates release engineering from acceptance of the installed household system. The owner requested the next development milestone to meet a production standard. The reviewed packaged candidate is still a development release; passing source tests alone does not establish that standard.

## iPhone runner diagnosis

PR59 at `c88c58e354e4da2dd831c5cc11447f406c41e1ed` had two failed native jobs, `103862281617` and `103862286300`. The first retained run passed the repository gate, native builds, geometry checks and 158 Swift tests. Its iPhone UI runner reached testmanagerd session setup but no UI test began before the existing 600-second test deadline. Its diagnostic reported `stage=xcode-test outcome=timeout`; the transport stage was skipped. Do not treat earlier stages passing as a passing native job.

The retained result selected iOS 26.2 while Xcode 16.4 supplied simulator SDK 18.5. The script previously chose the newest installed runtime without considering the selected toolchain. This is a concrete selection defect and a plausible contributor to the stall, not proof of its sole cause. A [first-hand runner-images report](https://github.com/actions/runner-images/issues/12777) describes a different simulator startup failure with the same Xcode/newer-runtime pattern.

[PR70](https://github.com/rdimascio/ellie/pull/70), final source `186c14fa8c1a375f19a094ef8aa9db674ef65f34`, adds strict version parsing, bounded platform diagnostics and a deterministic runtime policy. It prefers the newest available iOS runtime at or below the selected SDK; if none exists, it permits a runtime in the same SDK major generation. It excludes newer major generations. This is the runner's conservative selection policy, not a complete statement of Apple's support matrix.

A read-only MacBook inventory found Xcode 16.2, SDK 18.2 and the available iOS 18.3.1 runtime. That known-working test host exposed an overly restrictive first draft that rejected every runtime newer than the SDK. The follow-up preserves this same-generation case. Apple's [system requirements](https://developer.apple.com/xcode/system-requirements) separately describe SDK versions and supported simulators; an SDK number is not a universal simulator upper bound. No runtime installation or global Xcode selection was changed.

The first reviewed PR70 commit passed five focused cases and the complete gate (284 passed, one skipped). The final two-file selection follow-up passed the same five focused cases, lint and type checks. Root independently reran the five initial focused cases and the final two selector cases. These are synthetic command fixtures and pure selection tests, not a physical iPhone or Simulator run.

The existing iOS runner also signaled negative process-group IDs after its direct child closed. [PR71](https://github.com/rdimascio/ellie/pull/71), `c24ce2e8e50c90a7f06a41d00dbf9df1c7b5384b`, replaces that inherited path with retained direct-child handles, bounded TERM/KILL/reaping, read-only group absence probes and evidence retention on uncertainty. Cleanup failure fails the run; a retained result blocks a new run from overwriting it. The final eight focused cases passed independently under root review. The author's earlier full 286-pass/one-skip gate preceded the final test hardening and is not attributed to the final hash.

Root combined both reviewed histories at `4d9980b074fdca6e505ebb313680afbcd743458f`, resolved their shared diagnostic-fixture changes, and published that head in PR59. The combined ten focused cases and complete repository gate passed: **289 passed, one skipped**, with lint, formatting, contracts, type checks and web build passing. The full log is retained locally as `ellie-integration-4d9980b-full-check.log`.

The exact clean combined source then passed **two iPhone UI tests** on the physical MacBook's Simulator, using Xcode 16.2, SDK 18.2 and iOS 18.3.1. The run took 94,531 ms overall and reported successful shutdown, simulator deletion and derived-data removal. Root independently confirmed the exact created simulator, derived-data directory and result bundle were absent afterward. This is real Simulator execution on a physical Mac, not physical iPhone, microphone, LAN-command or installed-service acceptance. No test retry, runtime installation, global toolchain change or weaker UI assertion was used. The runner log is retained locally as `ellie-ios-runner-4d9980b-macbook.log`.

One further older-candidate native failure (`34809141787/103866784428`, still at `c88c58e3`) timed out during simulator boot readiness before xcodebuild started and reported shutdown/deletion failure. That separate pre-test failure is retained; it is not an application assertion and does not prove a unique cause for all CI stalls. GitHub native acceptance of the newly combined head remains a separate check.

## Restoration and final combined candidate

[PR72](https://github.com/rdimascio/ellie/pull/72), `ea8f0434cc9213b5d4276de028f211c6c0d20b8d`, publishes stopped legacy file restoration and explicit interrupted recovery. The final focused test passed on the Mini in 11.233 seconds and independently on the physical macOS 15.1 MacBook in 15.630 seconds. These tests use generated app fixtures, synthetic homes and fake unloaded-label observations. They do not start installed services or exercise Keychain, TCC or a desktop action.

Review required full validation of complete retained evidence, distinct partial-copy validation, writable app roots through rename, receipt removal last and repeated interrupted recovery. A proposed expansion to accept legacy roots at `0555` broke the pre-existing forward-recovery matrix on the MacBook; its log is retained as `ellie-legacy-review-555-macbook-failure.log`. That unsupported expansion was removed. Production migration preparation continues to accept the existing `0700` legacy contract. The final passing log is `ellie-legacy-final-macbook-pass.log`. Restoring files still reports runtime compatibility as unverified.

The combined runner commit `4d9980b0` exposed a deterministic Linux fixture failure in both GitHub TypeScript jobs: 278 passed, one failed, 11 skipped. The overflow fixture started `awk` under a shell, so the runner correctly retained evidence when the producer outlived its directly owned shell. Reviewed follow-up `a740228cd34417f1d2bc7cb2e1493e818dbd0952` uses `exec awk` to make the finite producer the direct child. It changes only the fixture; production cleanup and its uncertainty checks remain intact. PR71 had already been incorporated into PR59, so this later commit is carried directly in PR59 rather than being attributed to PR71's earlier diff.

The final combined source is `2f87a822db270a7059a8fe7f0a8598f9fc819c6a`, published in PR59. It preserves the reviewed PR70, PR71, Linux fixture follow-up and PR72 histories. Its exact full gate passed **289 tests, one skipped**, plus lint, formatting, contracts, type checks and web build. The log is `ellie-integration-2f87a822-full-check.log`. The shipping iOS runner and UI source are unchanged from the successful MacBook Simulator run above; no repeat of that same test is claimed. Current-head GitHub checks remain separate from the completed local gate.

The clean arm64 development ZIP is `EllieServices-0.1.0-dev-2f87a822-macos-arm64.zip`, SHA256 `a8738eb144a6ad314201cf31929f9f60bea8ac964d773b9d4385ed3107ccf882`. Root verified `sourceModified: false`, the exact source revision, the official Node 24.21.0 archive digest, 423 manifest files, 29 components and minimum macOS 14. The shipping inspector passed on the Mini and on the physical macOS 15.1 MacBook; the transferred ZIP digest matched. This verifies the actual distributed inspector's read-only operation. It does not stage, select, install or run household services.

At the final CI checkpoint, both TypeScript jobs for `2f87a822` passed, web jobs were running and native jobs queued. Four superseded unfinished runs at `4d9980b0` and `c24ce2e8` received cancellation requests after their exact heads were verified; completed results and retained failure logs were preserved. The current `a740228c` and `2f87a822` runs were not cancelled or retried. PR70 finished with one passing native run and one later speech-cancellation XCTest failure after its standard UI tests passed. That failure is the next diagnostic task, not a reason to claim the current native gate is green.

Read-only structured xcresult inspection on the MacBook identified that earlier ATS failure precisely: 22 of 23 tests passed; the cancellation test's shared `fixtureStatus` helper received HTTP 503 with `{"ok":false}` where lines 165–166 expected HTTP 200 with `{"ok":true}`. Either the `cancel/started` or `cancel/settled` five-second probe failed; the two call sites share assertions, so the result does not identify which. There was no XCTest crash or runner timeout. Changing production cancellation behavior is not justified by this evidence alone.

The ATS suite still selected iOS 26.2 independently of the standard UI runner's fixed selection. Its separate `scripts/test-ios-ats.mjs` also retains negative-PGID signaling paths. Before another owned MacBook ATS execution, apply the reviewed runtime-selection policy and the retained-direct-child cleanup contract to that runner, and add bounded phase-specific probe diagnostics. Keep the five-second probe bound, production assertions and no-retry behavior. The original artifact and structured summaries are retained; the temporary MacBook result-reader copy was removed.

## HTTPS runner follow-up

The first completed native job for exact `2f87a822` (`34811093665/103872435805`) passed all 158 Swift tests and both standard iPhone UI tests, selecting runtime 18.5. Its separate ATS runner still selected 26.2 under Xcode 16.4 / SDK 18.5. The saved result contains 23 tests, 22 passed and one failed: `NativeEnrollmentATSTests.testProductionTransportConnectsToPinnedLocalHTTPSUnderATS()` reported a bounded session-recovery timeout followed by `unavailable` after 3.6 seconds. All synthetic listener counters were zero. This is an executed XCTest transport failure, not a testmanager stall; the evidence does not distinguish networking, TLS setup or runtime instability. The result ZIP digest is `0cf15ce8ff5a97b4b60f0fd122816a7cce88fec9bc0e9348cd98546c68597a55`; structured summaries and the full job log remain retained locally.

[PR73](https://github.com/rdimascio/ellie/pull/73), `6b33e8c6e83d786f7caa726fa3c27dabb6a6fbfe`, gives cancellation and disconnect probes fixed phase-specific failure messages. Probe failure settles only the owned store or upload task and disposes its recording through the existing ownership checks. Coordinating review corrected an initial throwing path that could skip cleanup. Production cancellation, server probe bounds, success assertions and no-retry behavior remain intact.

[PR74](https://github.com/rdimascio/ellie/pull/74), `d30dc71`, applies the reviewed runtime policy to the separate HTTPS runner. It uses retained direct-child signaling, bounded process and component teardown, read-only group absence checks, closed platform diagnostics, evidence retention on uncertainty and refusal to overwrite retained results. Review also corrected test cleanup admission and added a finite fail-only watchdog to the synthetic speech worker, beyond its existing adapter deadline. The final eight finite regression tests passed independently; the author's complete Node suite passed 297 tests with one skipped. Earlier review snapshots are not attributed to the final watchdog and redaction additions.

The reviewed histories are combined and published in PR59 at `2867877b1bfb01ca9305151f0f1c20526a24f15b`. The exact full repository gate passed **297 tests, one skipped**, plus lint, formatting, contracts, type checks and web build in `ellie-integration-2867877b-full-check.log`. Relative to `2f87a822`, only the HTTPS runner and two test files changed. The previously inspected package remains the exact `2f87a822` artifact; no new ZIP or installed acceptance is inferred from this test-only follow-up.

The exact clean combined source passed the actual app-hosted HTTPS suite on the physical MacBook's Simulator with Xcode 16.2 / SDK 18.2 / runtime 18.3.1. The runner reported 60,815 ms through successful validation and xcodebuild reported 24.703 seconds for test execution. Production pinned HTTPS, synthetic speech and cancellation, rejection cases, Simulator Keychain operations and built-app network policy passed. Listener request and response counts were exactly session 1, inventory 2, command 1, logout 1, unexpected 0; the existing five-upload/process-marker assertions also passed. No retry, physical microphone, physical iPhone or household desktop action was involved. The complete log is retained as `ellie-ats-2867877b-macbook.log`.

The runner exited successfully after cleanup. Root independently verified that its exact created Simulator and result bundle were absent. A read-only temporary-root inventory found only one older retained ATS root, whose creation and modification predated this run; that older evidence was preserved. The owned source archive/extraction remains available. This passing run supports the reviewed change but does not prove the cause or elimination of intermittent GitHub failures.

On September 14, both complete GitHub runs for exact `2867877b` passed: [34813193639](https://github.com/rdimascio/ellie/actions/runs/34813193639) and [34813197685](https://github.com/rdimascio/ellie/actions/runs/34813197685). Each native job passed 158 Swift tests, two standard iPhone UI tests and the app-hosted HTTPS/speech suite. Both runners selected iOS 18.5 under Xcode 16.4 / SDK 18.5. HTTPS request and response counts matched the expected finite operations; all TypeScript, web and security checks also passed. Full native logs are retained as `ellie-2867877b-native-103878426861.log` and `ellie-2867877b-native-103878438426.log`. This closes the current-head CI gate without a retry, production deployment or claim that intermittent failures cannot recur.

## Node hardening preparation

[PR75](https://github.com/rdimascio/ellie/pull/75), `aa1eff9de3a1f5199a62f91b10338966802f9985`, adds an isolated Node hardening harness on top of the passing PR59 source. Coordinating review required bounded streaming extraction, exact archive/member/runtime version correlation, semantic entitlement decoding, deterministic workload assertions, and cleanup that preserves evidence when owned children have not settled. The final eight focused regressions passed. The exact implementation passed the complete repository gate: **305 tests, one skipped**, plus lint, formatting, contracts, type checks and web build. The retained full log is `ellie-node-hardening-root-final-check.log`. GitHub acceptance of this new PR is still separate from the completed local gate.

Root also ran the final harness on the physical Mini with the checksum-verified official Node 24.21.0 arm64 archive. Its isolated ad-hoc hardened copy passed V8 warmup, worker threads, the known SHA-256 result, dynamic standard-module import and synthetic loopback TLS. The successful run removed its owned temporary root and retained no failure evidence. Its log is `ellie-node-hardening-root-final-mini.log`. No installed service, credential or permission changed. This is runtime compatibility evidence for that workload; it does not prove observed optimizing compilation, Developer ID library validation, notarization, native add-on compatibility or installed service behavior.

Both exact-head GitHub runs subsequently passed: [34818829406](https://github.com/rdimascio/ellie/actions/runs/34818829406) and [34818863202](https://github.com/rdimascio/ellie/actions/runs/34818863202). Each native job passed 158 Swift tests, two standard iPhone UI tests and app-hosted HTTPS/speech checks, with both runners selecting iOS 18.5 under Xcode 16.4 / SDK 18.5 and the expected finite request counts. All Linux TypeScript, browser and security checks passed. The native logs are retained as `ellie-aa1eff9-native-103895432232.log` and `ellie-aa1eff9-native-103895536788.log`. Root fast-forwarded PR59 to the identical `aa1eff9` source, preserving PR75's history; no new source test or shipping ZIP is inferred from that incorporation.

The next implementation boundary follows the [authenticated distribution design](../service-distribution-trust.md). Public installation must remain unavailable until receipts, selection, recovery and launch all bind the authenticated manifest and immutable trusted policy. A positive synthetic signature test cannot establish publisher authenticity.

## Manifest authorization and signed runtime preparation — September 14

[PR76](https://github.com/rdimascio/ellie/pull/76), `acbf142cf989f6bff663d900918b89ad84b0459b`, adds read-only manifest envelope authentication against an independent Team ID and fixed Developer ID requirement. It validates required sealed resources, exact external bytes and the canonical trusted policy binding. The returned copied bytes are an `AuthenticatedEnvelope`, not payload or installation authorization. The exact implementation passed the complete local gate: **306 passed, one skipped**, plus lint, formatting, contracts, type checks and web build. The focused native test passed actual Security.framework verification of an ad-hoc fixture in a test-only binary and rejection by the shipping binary. Its fixed negative results, canonical paths, resource mutations and substituted final inode use synthetic fixtures on the physical Mini. No actual Developer ID publisher, notarization, payload inventory, concurrent hostile race or installed service acceptance is claimed. Log: `ellie-service-authorization-root-final-check.log`.

[PR77](https://github.com/rdimascio/ellie/pull/77), `37c9266589df2c90259c5dc69f0da905d5534312`, adds the explicit Developer ID mode to the isolated Node harness. The final reviewed runner SHA-256 is `0d53903487e8133c9e9e42a45e218323066c5623ad2356d5e023c646ff1b2f2c`. Its exact final gate passed **310 tests, one skipped**, plus lint, formatting, contracts, type checks and web build; thirteen focused regressions include an actual macOS requirement-parser rejection with a synthetic Team ID. Log: `ellie-node-developer-id-publish-final-check.log`. The signing identity is supplied privately at invocation time. Only signing uses the validated caller HOME; the workload remains isolated. Known failures have fixed redacted classes.

Two authorized physical macOS15.1 arm64 MacBook attempts failed before the workload ran. The first used runner SHA-256 `05a97bc94bf285d5417a8a84d08dbc9d0a4877d1c44fa3677ea4a71dfe103ee2`; read-only diagnosis found that the owned HOME hid the existing identity. The second used corrected runner `2aa84a6b3adbf88ccee515441087e15e9e003322ba4f05d26c419caeb978720e` and still failed. Fresh read-only default-Keychain status plus explicit identity inventory established that the selected identity is in that locked Keychain. Both earlier runners shared one diagnostic stage for signing and verification, so the failure does not distinguish those commands; final source separates the stages. The owner has been asked to unlock the Keychain before another attempt. No password, private key, ACL, search list, TCC grant or household service was changed. Failed logs `ellie-node-developer-id-final-macbook.log` and `ellie-node-developer-id-reviewed-macbook.log` and the owned remote staging evidence remain retained. No successful Developer ID, notarization or installed-service acceptance is claimed.

Both PRs have coordinating and independent final review clearance. Neither is incorporated or deployed. PR59 remains `aa1eff9`; the development ZIP remains `2f87a822`. The latest follow-up and CI status are recorded below.

A newly inspected branch-initialization run [34821611275](https://github.com/rdimascio/ellie/actions/runs/34821611275), at unchanged `aa1eff9` before PR76 implementation, failed native job `103904240266`. The test `native lifecycle validates selection and sends only fixed bounded launchctl operations` exceeded its 20-second test deadline (reported 20,323 ms); subsequent app build, Swift and iPhone steps were skipped. Linux TypeScript and browser checks passed. Three later same-source runs passed: [34821680489](https://github.com/rdimascio/ellie/actions/runs/34821680489), [34821686334](https://github.com/rdimascio/ellie/actions/runs/34821686334) and [34821814205](https://github.com/rdimascio/ellie/actions/runs/34821814205). Retained failure log: `ellie-aa1eff9-34821611275-failed.log`. This is separate from the earlier iOS timeout; no root cause or fix is yet established. Read-only timeout-boundary review is in progress without a blind rerun.

Review established that the lifecycle test's outer budget includes a fresh Swift compile, signing, fixture setup, many bounded subprocesses, an intentional two-second deadline case and a 500 ms lock-holder case. The failed log does not identify which operation was slow. PR76 follow-up `a068cff44d37658fce8e73cee25c57787fec7bdc` changes only that aggregate budget to 30 seconds, preserving all product deadlines and assertions. Focused 1/1 passed in 9.312 seconds; final complete gate **306 passed, one skipped**, plus lint, formatting, contracts, type checks and web build. Log `ellie-service-authorization-lifecycle-final-check.log`. Both current Linux/browser runs and security passed; native checks are pending.

PR77 run [34824934866](https://github.com/rdimascio/ellie/actions/runs/34824934866), job `103914769256`, failed the rollback test during real browser-certificate generation before persistence. Its collapsed error does not identify which generated certificate predicate failed. Retained log: `ellie-pr77-37c9266-typescript-103914769256.log`. Another same-head Linux run passed, as did both browser runs, security and native job `103914781208` in [34824938746](https://github.com/rdimascio/ellie/actions/runs/34824938746). The passing native log `ellie-pr77-37c9266-native-103914781208.log` records 158 Swift tests, two standard iPhone UI tests and app-hosted HTTPS/speech checks with the expected finite request counts under Xcode16.4/SDK18.5/runtime18.5. The other native job remains pending; PR77 must not be integrated while its failed check is unresolved.

[PR78](https://github.com/rdimascio/ellie/pull/78), `1cdfde26dba0c3bcf61375a076915b10d60ca08a`, separately adds eight fixed redacted browser identity validation categories and removes incidental OpenSSL generation from that rollback test. Dedicated generation tests remain, with actual temporary fixture mutations proving malformed certificate, wrong leaf key, missing profile extension and wrong-CA signature rejection. Every OpenSSL test subprocess is bounded to 30 seconds and 64 KiB. Final focused **16/16** and full **306 passed, one skipped**, plus lint, formatting, contracts, type checks and web build; coordinating and independent review passed. Log `ellie-browser-tls-diagnostics-root-final-check.log`. This is synthetic temporary identity testing on the Mini, not a live certificate change or a reproduction of the original rejected predicate. CI is pending. After its checks pass, apply the reviewed fix to the failed branch and validate that updated source before integration.

The independently reviewed [manifest-v2 inspection contract](../service-distribution-trust.md#next-implementation-contract-authenticated-payload-inspection) is the next ready implementation. It specifies the five current native components, exact nested role identifiers, closed entitlement values and distinct envelope/payload policy bindings. No implementation, production builder or installation authorization is inferred from the design.

## Remaining release gates

- Complete installed migration and restoration acceptance. The reviewed stopped file transaction is implemented, but restoring app/plist files does not restore an external checkout, dependency tree, state schema or permission grant.
- Preserve the passing exact-head CI evidence above; new implementation requires its own relevant checks. Retain any new failure before deciding another run.
- Exercise the shipping packaged services in a fresh logged-in macOS user or VM, or through a separately authorized installed migration. A temporary HOME does not isolate launchd's fixed labels. Verify actual identity initialization, Keychain access, Accessibility, readiness, login/restart, sleep/wake and one explicit cross-Mac action.
- Add and validate production signing, notarization, stapling and Gatekeeper acceptance. The current builder uses a development ad-hoc policy; the read-only envelope command is not production install authorization. A read-only inventory on the current Mini found no available signing identity in that execution context. The physical MacBook does have the selected Developer ID identity, but its default Keychain was locked when checked; a positive signing experiment awaits owner unlock.

The owner's earlier successful Arc/Safari, terminal-free operation, offline-row and MacBook sleep/wake checks remain accepted for the existing checkout installation. They are not acceptance of a new packaged artifact and do not need to be repeated for this source-only review. See the [owner acceptance record](2026-09-13-native-cross-mac-owner-acceptance.md).

The earlier [inference record](https://github.com/rdimascio/ellie/blob/c88c58e354e4da2dd831c5cc11447f406c41e1ed/docs/validation/2026-09-12-inference.md) documents bounded physical direct-adapter, coordinator/LAN scheduling, two-worker placement and cancellation checks with temporary synthetic fixtures. Do not claim inference has never been hardware-tested. Those checks do not establish sustained load, installed-model operation, crash/network/sleep resilience or distributed MLX acceptance.

## Concurrent ownership

The owner separately authorized a life-harness task in `codex/life-harness-build`, based on the reviewed packaged candidate. That task owns additive personal knowledge, tasks/routines and conversational features in its own worktree. This release lane owns installer, migration and iOS test-runner reliability. Neither task's authorization permits the other to deploy or merge the open release stack to main. New durable schemas need explicit versioning; earlier core-state rollback evidence does not cover future populated harness stores.

The live checkout, existing identities, Keychain items, TCC grants, certificates and installed services were preserved during this review. No new phone prompt is pending.
