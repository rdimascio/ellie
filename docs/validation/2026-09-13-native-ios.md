# Native iPhone dashboard validation — 2026-09-13

The iOS 18.2 SDK built the `EllieIOS` app with no external dependencies. A dedicated iPhone 16 simulator running iOS 18.3.1 exercised `EllieIOSUITests`: it edits a note, relaunches to verify persistence, creates and renames a dashboard through native controls, deletes it and verifies navigation returns to the dashboard list.

The original simulator workflow passed one test with zero failures. All ten shared macOS model/store tests passed on the MacBook with full Xcode, including explicit UTC versus Los Angeles clock formatting. The Node 24/Bun 1.4.2 full check passed 162 tests with one platform skip, plus lint, formatting, contract generation checks, type checking and the browser compatibility build.

The validation checkout, derived data and simulator were temporary. The bounded test runner creates and deletes only its uniquely identified simulator. No installed Ellie service, `~/ellie-test`, existing simulator, physical phone, signing identity, Keychain item or browser identity was read or changed.

Reproduction commands are in [the iPhone development guide](../native-ios.md). The runner selects a compatible installed iOS 17-or-newer runtime and iPhone type, creates a uniquely named simulator and private derived-data directory, bounds every subprocess, and removes owned resources after each run. A failure retains its synthetic XCTest result at `test-results/native-ios.xcresult`; CI uploads it for three days. Physical-device layout, Files-provider round trip, signing, distribution and an accessibility review remain untested. The shared macOS XCTest suite covers schema validation, persistence, import safety and dashboard/widget mutations.

The native enrollment foundation was validated on 2026-09-13 with `bun run check` under Node 24 and Bun 1.4.2. Synthetic tests cover the canonical bounded QR contract, exact native request channel, controller-only management, single-use pairing and candidate-token recovery, private-file reopen, persistence failure, expiry, logout and revocation. Tests use temporary private state directories and synthetic loopback TLS only. They do not access an installed coordinator, real credentials, Keychain, a camera or an iPhone.

## Native enrollment client

The scoped iOS client adds canonical shared-fixture parsing across 6 valid and 34 invalid cases, explicit origin and grant confirmation, a bounded pinned transport, durable pending recovery and one atomic app-specific Keychain envelope. All 28 current macOS tests passed: 10 dashboard tests and 18 native enrollment tests. Synthetic Swift tests cover no work before an explicit action, no invitation persistence, no pairing replay after uncertainty, rejection cleanup, late completion cancellation, bounded clock skew, exact pin and hostname checks, past/future certificate rejection, absolute slow-response deadlines, cancellation, oversized responses, and redirects. The Node 24/Bun 1.4.2 full check passed 182 tests with one platform skip.

A separate iPhone 16 simulator workflow opened the Coordinator screen, verified that enrollment remained behind the explicit Scan button, and returned without opening the scanner or displaying a consent prompt. An earlier combined run passed that test but failed the dashboard workflow: tapping the `Kitchen` static text did not navigate from the dashboard list, so XCTest could not find the subsequent `Dashboard options` button. The failure result remains retained on the validation MacBook. Earlier runs also exposed an ambiguous Save selector and dropped bulk keyboard input.

The UI test now activates the dashboard navigation button, verifies the destination, scopes rename confirmation to its alert, and checks the field value after each character. The final combined run passed both UI tests with zero failures in 50.688 seconds, preserving note persistence, dashboard create/rename/delete and Coordinator navigation assertions. Its sanitized log is `/tmp/ellie-ios-ui-verified.QK0fsClj-ui.log` on the validation MacBook. The successful runner removed only its owned simulator, derived data and result bundle.

The macOS Swift HTTPS suite does not establish acceptance under the iOS app's App Transport Security policy. A separate final app-hosted run on an iPhone 16 simulator with iOS 18.3.1 passed all five XCTest methods: one production transport method covering pinned loopback HTTPS, wrong-pin rejection and wrong-hostname rejection, plus four real Keychain API lifecycle methods. The Keychain tests use UUID-scoped synthetic services and exact accounts, covering reopen, atomic promotion, device-only accessibility, removal, malformed-envelope recovery and preservation of a second owned identity. They never query the production enrollment service or owner credentials.

The hardened runner also verified that only one valid native authenticated request reached the fixture and that the built app policy contains exactly `NSAllowsLocalNetworking: true`, with no arbitrary-load exception or fixture settings. Production URLSession requires TLS 1.2 or newer. Earlier app-hosted attempts included missing test configuration, an underspecified synthetic leaf certificate rejected by iOS, and XCTest async-autoclosure compilation errors; these were corrected before the final passing run. The final sanitized log is `/tmp/ellie-ios-ats-final4-e31af8.log` on the validation MacBook. The runner cleaned only its owned simulator, loopback server, certificate/key, derived data and successful result bundle.

This demonstrates the production transport and Keychain APIs inside the simulator app. It does not establish physical iPhone enrollment, device Keychain behavior, local-network consent or camera permission/interruption acceptance. Native app command dispatch remains unimplemented.
