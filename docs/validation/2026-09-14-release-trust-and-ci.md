# Release trust and CI checkpoint — September 14

The release candidate remains a development artifact. This record supersedes pending-state statements in the earlier production-readiness review, while preserving their historical evidence.

## Reviewed integration

PR59 is now `ac1aef4707f88b3dc3fe988a8b2dcff4a9a8b201`. PR82 passed coordinating review and both complete original CI runs, then was fast-forwarded into PR59 with its history preserved. GitHub recorded PR82 merged at 11:07:17 UTC on September 14. No main merge or household deployment occurred.

Native job `103940768474` in run `34833110048` checked out exact `ac1aef4`; job `103940850672` in run `34833137055` checked out merge `4c708c4ee1299f7434ff872c1374058930d61070`. Both have tracked tree `fce9e4ab0bf6450358877b313b9da783a4973daa`. Both complete runs passed, including 158 Swift tests, two standard iPhone UI tests, app-hosted HTTPS/speech checks, expected endpoint counts and reported cleanup. Subsequent integration CI is separate evidence. Runs `34836563451` (job `103951615581`, exact `ac1aef4`) and `34836568335` (job `103951629527`, merge `dbfc5d23fa913f1fd93a0c2cf78a9298d624edbd`) also passed the full native suite with the same tracked tree. Run `34836569965`, job `103951636383`, checked out exact `ac1aef4` and failed the older standard UI helper: it had already entered `Remember the e mugu` before Save, then persisted that same text. Swift 158 passed, but HTTPS was not reached. The retained log `ellie-pr59-ac1aef4-native-103951636383.log` has SHA-256 `1fa1d320b112d1d91aaa9b6efde7e6e0384359dd842c89259fcaf116e50244f7`. This later failure does not invalidate the earlier dashboard regression evidence, but the candidate is not wholly green.

The preceding PR76 integration was `a068cff44d37658fce8e73cee25c57787fec7bdc`. Checkout provenance was inspected in the actual native job logs. Job `103916997811` checked out `a068cff`; job `103917005133` checked out merge `32a93c2`. Both have the identical tracked tree `d9b4db97dc8e0e337a3928d3822478e41f3d1496`. Each passed 158 Swift tests, two standard iPhone UI tests and the app-hosted HTTPS/speech suite. These are content-equivalent CI passes, not identical checkout commits. Subsequent failures on that content are recorded below; the earlier passes do not establish that intermittent failures are eliminated.

## Retained failures and focused follow-ups

PR77 remains `37c9266`, and PR78 remains `1cdfde2`; neither is integrated. In addition to PR77's previously retained Linux certificate-generation failure, native job `103914769068` saved an incomplete note. PR78 job `103918001852` also saved incomplete text, while job `103917989213` failed the disconnect-start probe. The two PR78 native checkouts differ in history but share tracked tree `abafd2b392c7f2826b25ccd5af0d6de0df569b5b`.

The retained PR78 HTTPS result contains 23 cases: dashboard 12, credential vault 4, ATS 1 and speech 6. Exactly one speech case failed at the five-second disconnect-start acknowledgement. XCTest executed; this was not a test launch timeout or established ATS-policy failure. Existing counts omit the relevant speech route details, so the underlying upload/worker cause remains unknown. The original result ZIP SHA-256 is `60562ee800e426613c25f52447f607b64861578cdaad5aed12c4beb1f6383ca0`; it and its extracted result remain retained locally.

[PR79](https://github.com/rdimascio/ellie/pull/79), `8e45f4ffa12fe2b24962ca93d253dd1d34e8f2fb`, waits for exact cumulative input and SwiftUI note-count state before Save. Its owned MacBook Simulator run passed both UI cases, including exact note persistence after relaunch and dashboard rename/deletion. Xcode 16.2, SDK 18.2 and iOS 18.3.1 were used; no physical iPhone or household installation was tested. The created Simulator and owned source/derived roots were removed and their absence independently verified. The retained log `ellie-ios-input-state-sync-macbook.log` has SHA-256 `425f803e8a5568b6fe756d6dce1983702c113fede541c0b5ac75b767d3630573`.

PR79 CI run `34830272383` passed. Run `34830268379`, native job `103931720739`, failed its two-second exact-input predicate after entering the second chunk. A later diagnostic read showed `Remember`; the `Optional(...)` wrapper is Swift formatting and does not establish a value-format defect. The predicate deadline and later read are separate observations. Its exact source is `8e45f4f`, tree `3398cbea0648edd6ab57498dcc21941704e0f4aa`. The result is retained. Follow-up `e606a6b` now records values inside an explicit String predicate separately from the later diagnostic read, preserving its two-second bound and all assertions. That changed source passed both workflows once on the MacBook Simulator in 62.135 seconds (runner 110.564 seconds), using the same Xcode/SDK/runtime. Root independently verified absence of its created Simulator, source and derived roots. Retained log `ellie-ios-predicate-review-final.log`, SHA-256 `33cafc025bbe4052ce034e5647920b0a593b6c94347b49c1d52fda0e34f1b887`. At `e606a6b`, run `34832401803` passed the full native suite. Run `34832403872`, job `103938515863`, passed all 158 Swift tests and both standard UI cases, then failed the older app-hosted dashboard canceled-read test. Its actual merge `647e71726f0c6709c9b65faa965698864a9a4c9b` has the same tracked tree `47e489020a97abd23c3faa2816adfbab817e4dd8` as `e606a6b`. The structured result ZIP is retained with SHA-256 `810344ad3d3441f5bb321697111929e118e4b96b00070eecf5d792fc0929bc30`. The MacBook was unreachable during this artifact audit, so no new remote reader was created and no exact failed assertion values are claimed for this result. Root merged the reviewed PR82 correction into PR79 without conflicts and pushed `ff6740becede1ca1867cd8d4fee9570cc9126a95`; both new native runs failed and remain unintegrated. Their evidence is detailed below; the earlier hosted input failure cause remains unknown.

At PR79 `ff6740b`, native job `103952189583` in run `34836747693` checked out that exact commit. All 158 Swift tests passed; the standard UI note test then failed its two-second predicate after one evaluation even though that evaluation and a later read both recorded `Reme`. The log does not record when the cross-process value read completed, so this is not proof of an input mismatch or a reason to weaken the assertion. Log `ellie-pr79-ff6740b-native-103952189583.log`, SHA-256 `8f7dce3737befe9bfbe8f168058a8200a7dc4e21175d71063c2fe6df84d0a8cc`; original UI result ZIP SHA-256 `85e22a0a1fd25f79f16cab27765c6c9584dc61e6ea709545eaa3589fb774db35`.

Native job `103952201786` in run `34836751836` checked out merge `646150f95a0671f810d8b43f06f2ecb1d3515883`. It and exact `ff6740b` share tracked tree `6120245ef16b7baaab436cd5af25b8a04f2bfc83`. Swift 158 and both standard UI workflows passed. The app-hosted result reports `NativeEnrollmentATSTests.testProductionTransportConnectsToPinnedLocalHTTPSUnderATS` failed; session/inventory/command/logout request and response counters were all zero. The runner reported completed fixture teardown, Simulator shutdown/deletion and owned-root removal. The exact XCTest error requires structured-result inspection; zero requests alone does not establish an ATS-policy, TLS or launch cause. Log `ellie-pr79-ff6740b-native-103952201786.log`, SHA-256 `7022d78b1eb85ee8784cb418a17152d9e238322f1e80782d593bf44a88043692`; original transport result ZIP SHA-256 `23c097d5de7cbd043958d76410b2d7ae73a6a729797a2ca14f8e0daf3eb9bce4`. Both ZIPs are retained locally. No failing run was retried.

[PR80](https://github.com/rdimascio/ellie/pull/80), `dc52d9955f59e23d010024bd4cf6973677f8a6f3`, adds capped, redacted speech lifecycle evidence and fixed control-timeout reasons. Wrapper entry does not imply completed authorization. It preserves readiness conditions, the five-second deadline, cancellation assertions and owned cleanup. Fifteen focused checks and the complete repository gate passed: **313 tests passed, one skipped**, plus lint, formatting, contracts, types and web build. Log: `ellie-speech-start-diagnostics-publish-final-check.log`. This is synthetic diagnostic validation. Native job `103935731689` in run `34831526615` checked out exact `dc52d99` and later failed the older standard UI helper with already-mangled input before Save, then the same saved/relaunched text. It did not reach the new speech diagnostics. Retained log `ellie-pr80-dc52d99-native-103935731689.log`, SHA-256 `564aec53d9a1c09b33af69f99eafb04309f1fb874568f4bc4ed2cc51e5925759`. Native job `103935802782` in run `34831548183` passed the full suite, including the new speech summary: five uploads, starts, exits and settled turns, zero active turns and no control timeout. Its checkout merge `cccc374a99e4d3522c994d76854d0cc4e46e878a` has the same tracked tree `820004491e6a256577f9cc1bc423633caab2cb81` as `dc52d99`. Log: `ellie-pr80-dc52d99-native-103935802782-pass.log`. The earlier speech failure's cause is not claimed fixed; PR80 cannot be integrated with this failed check unresolved.

Additional original runs of the integration content exposed two separate failures. Job `103928679319` in run `34829328836` checked out exact `a068cff` and hit the native selection fixture's 20-second aggregate deadline; the inner slow operation is unknown. Job `103928947566` in run `34829411226` checked out merge `61312c4` with the same `d9b4db9` tree. Both standard UI tests passed; the app-hosted `DashboardSyncTests.testCancelledOldReadCannotReplaceNewerRead` failed. Its retained structured result reports revision 3 and the initial dashboards where revision 9 and an empty dashboard list were expected. The test used one `Task.yield()` to assume the first mocked read had begun; that does not guarantee entry before cancellation. PR82 now supplies the reviewed gated ordering correction, with no production state behavior change. The result is compatible with this concrete scheduling defect; the failed run did not directly trace its internal ordering. The original ZIP SHA-256 is `cee1a5b83eba0adbd4ddc2902e64ecaf01168e0852ad793b636c31e08eec3da7`. Do not classify it as ATS failure merely because it runs in the HTTPS harness.

The three new logs are retained with run and job identifiers: `ellie-run-34830268379-job-103931720739.log`, `ellie-run-34829411226-job-103928947566.log`, and `ellie-run-34829328836-job-103928679319.log`. No failing run was blindly retried.

[PR82](https://github.com/rdimascio/ellie/pull/82), `ac1aef4`, publishes the deterministic dashboard read-order regression after coordinating and independent review. Both request entries are bounded, revision 9 must be applied before revision 3 is released, and timeout paths release gates and await captured tasks. The full MacBook Swift suite passed 158 tests before the final cleanup-only branch, then all 12 focused dashboard tests passed that final change. An owned source mutation removing the actual production generation guard made the same test fail with stale revision 3. Logs: `ellie-dashboard-read-order-final-swift.log`, `ellie-dashboard-read-order-final-cleanup-focused.log`, and `ellie-dashboard-read-order-negative-control.log`. An earlier full attempt lacking the Node 24 PATH failed synthetic network fixture startup; its log `ellie-dashboard-read-order-full.log` remains separately retained. Production code and household services were unchanged. Both complete CI runs passed and this source is now integrated into PR59; their exact provenance is recorded above.

## Distribution and installed acceptance

[PR81](https://github.com/rdimascio/ellie/pull/81), `da32f34db601a1a5fdb80aeddb091689838cfb58`, publishes full manifest-v2 payload inspection on the separate push-verified `codex/authenticated-payload-inspection` branch after coordinating and independent review. It preserves development v1, requires the five fixed native components and closed boolean entitlement objects, and returns separate envelope/payload/manifest digests without installation authority. Review required exact CFBoolean checks on actual signing metadata because Foundation can cast numeric 1 to Bool, plus repeated file and payload-directory metadata checks. The final exact-source complete gate passed **307 tests, one skipped**, plus lint, formatting, contracts, types and web build; log `ellie-authenticated-payload-publish-final-check.log`. Real ad-hoc signature fixtures cover integer-valued entitlement rejection, exact native policy and payload mutations. Native job `103937795248` in run `34832175320` subsequently hit the existing migration-preparation test’s 30-second aggregate deadline at 30,324.8 ms; the new payload inspection passed. Its actual checkout was merge `10a24fe6d503e181094cc8c6d860a9b7274764e7`, with the same tracked tree `900c062a4059f62bc0e19faa2bfb6a8a6d590173` as `da32f34`. No inner slow operation was logged. Retained log `ellie-pr81-da32f34-native-103937795248.log`, SHA-256 `c8dddc73062e480036fdde794d28a1187c5713ae8c1174b61e93f3da11b45ae5`. Native job `103937692818` in run `34832142776` checked out exact `da32f34` and passed the full suite, including 158 Swift tests, two standard UI tests and app-hosted HTTPS/speech checks under Xcode 16.4, SDK/runtime 18.5. Log: `ellie-pr81-da32f34-native-103937692818-pass.log`; PR81 must not be integrated while its failed check is unresolved. This does not establish Developer ID authenticity, notarization or installation acceptance.

Physical Developer ID runtime validation still awaits the owner's already-requested login-Keychain unlock. No further signing attempt or credential/permission change occurred. Installed packaged acceptance still needs an isolated logged-in GUI context or separately authorized migration. The inspected shipping ZIP remains the development `2f87a822` artifact; no new ZIP or installed acceptance follows from these source changes.

Existing owner-reported Arc/Safari, terminal-free, offline-row and sleep/wake checks remain accepted for the checkout-backed installation. They are not transferred to this package. Earlier bounded hardware inference records also remain intact; sustained installed-model and distributed MLX acceptance are not established.

## Compiled installer fixtures

[PR83](https://github.com/rdimascio/ellie/pull/83), `e117f880cd2d6e2f1d2ef497ca9b42ca84597ca5`, implements one process-local compiled template with isolated mutable scenario copies. The initial branch was pushed from PR81 before edits. Root review tightened immediate directory identity capture, current-user ownership, exact sealed modes, before/after input hashes including generated source, bounded temporary-source cleanup and retained failure paths. Production code and all existing scenario deadlines remain unchanged. Source hash and exact validation are in [the fixture record](https://github.com/rdimascio/ellie/blob/e117f880cd2d6e2f1d2ef497ca9b42ca84597ca5/docs/validation/2026-09-14-native-fixture-template.md).

The final physical Mini gate passed **308 tests, one skipped**, plus lint, formatting, contracts, types and web build. The Node suite took 42.509 seconds; setup/isolation took 5.699 seconds and migration preparation took 7.470 seconds. Log `ellie-native-fixture-template-root-final-check.log`, SHA-256 `102b7909359721722deeb9e70858f1be2b900953c798ff1dcb7f6bbd02d38331`. This is synthetic fixture validation with real native compilation/signing on macOS 26.6.2 arm64, not installed-service or MacBook acceptance. New GitHub CI is pending. Repeated compilation was removed as concrete fixed work; the exact slow operation in earlier CI failures remains unknown.

## Authenticated capture implementation

The [capture contract](../service-authenticated-capture.md) has coordinating and independent review. Final corrections require prospective namespace capacity before mutation, complete CLI grammar before opening state, safe partial-stage preservation without blocking unrelated captures, and bounded descriptor ownership. Named recovery needs no external source; publication confers no selection or launch authority.

The untouched `codex/authenticated-candidate-capture` branch was pushed at `da32f34` before initial authorization-copy changes in its separate worktree. The implementation is incomplete and unpublished. Parallel agents then stopped with usage-limit errors; root finished the fixture review and publication locally. Resume the retained capture work rather than recreating the branch, and merge the fixture history before updating its compiler source list. No running background implementation agent is claimed by this checkpoint.
