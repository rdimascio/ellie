# Native pairing management validation — 2026-09-13

This slice was validated with synthetic identities and responses only. It did not access a live coordinator, LAN service, controller credential, Keychain item, phone, camera, or account.

`xcrun swift build --package-path apps/desktop` passed locally. On the authorized MacBook, an isolated copy under `/tmp/ellie-pairing-validation-ecbd7bca-94ae-4987-88e4-a66d29dca497` passed the focused `PairingManagementTests` (1 test) and the full Swift suite (84 tests) with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`; the full suite required Node 24 on `PATH` for its existing TLS fixture. The owned remote directory was removed. The synthetic test covers explicit list, invite grant construction, QR hiding semantics, and revoke without replay. It does not prove optical QR scanning or a physical LAN enrollment.

With local Node 24.21.0 and Bun 1.4.2, `bun install --frozen-lockfile` and `bun run check` passed lint, format, contract generation, TypeScript, the Node test suite, and the Vite build.

An isolated ad-hoc signed QA app was built at `/tmp/ellie-pairing-qa-5d6d1c3e-7b39-47f9-8726-6c52fdfe32d6/Ellie Pairing QA.app` with bundle identifier `org.ellie.pairing-qa`. Its compiled fixture supplies one eligible synthetic Mac and one synthetic phone through injected protocol clients. It was prepared for attended native UI review and was not launched by this task.

After the strict-contract and lifecycle hardening review, the focused MacBook suites passed 5 `PairingManagementTests`, 16 `CoordinatorTransportTests`, and 7 real `CoordinatorNetworkTests`. They cover a controller-delegated listener on a distinct host, port, and leaf pin; malformed pin, changed grant, noncanonical origin and extra-field rejection; exact client roles, grants and lifetime; visible-code revocation cancellation; 100 immediate transport cancellations; wrong HTTP response media type; and Vision decoding of the maximum 2,300-byte QR with its four-module quiet zone. A generic desktop debug build and an ad-hoc signed release build also passed. The first bounded full suite passed all 90 tests in 11.117 seconds with Node 24 on `PATH`.

The publication snapshot then tightened hexadecimal pin and identifier parsing, added terminal LF, CR, and CRLF identifier coverage plus an escaped-newline pin case, and classified an oversized successful invitation response as outcome-unknown. Its focused pairing suite contains 6 tests. A fresh bounded full-suite run on the authorized MacBook passed all 91 tests in 10.600 seconds with Node 24 and full Xcode.

An earlier full-suite attempt reported six `CoordinatorNetworkTests` fixture exits before they accepted a request. Reproduction with the same remote shell showed the exact cause: `/usr/bin/env` could not find `node` because that invocation omitted the required Node 24 path. With the installed Node 24 binary directory restored to `PATH`, all seven real TLS tests and the full suite passed. This was separate from PR 35's native-enrollment fixture cleanup.

The refreshed synthetic review app is `/tmp/Ellie-Pairing-QA.app` on the authorized MacBook. It uses only compiled in-memory fixtures with 16 eligible Macs and 18 active iPhones, makes no network or credential calls, and has bundle identifier `org.ellie.pairing-qa`. An attended review on the Mac mini used its copied `/tmp/Ellie-Pairing-Review-QA.app`: the reviewer entered a label, selected `mac-1`, rendered the maximum-size QR with a visible white quiet zone and exact grant/expiry, hid it and saw the still-valid warning, scrolled to the eighteenth client, revoked only that client, and confirmed the Done control remained visible. The app was then quit. This validates the native GUI with injected content; optical scanning and a physical enrollment remain untested.
