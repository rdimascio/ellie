# Household assistant delivery queue

This is the working queue for the household alpha. Each completion advances the next ready slice. CI delays, absent OAuth credentials, and attended phone steps block their own items, not independent implementation. Keep at most three implementation agents active, with one coordinating review. Keep changes independently reviewable and distinguish implementation, simulated tests, real hardware, and user-reported acceptance.

## Current delivery

| Item                        | Owner / branch                           | State                                      | Acceptance                                                                                                                                                                                                                                               |
| --------------------------- | ---------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Granted phone app opening   | PR #19, `codex/phone-control-demo`       | Implemented; user reports phone demo works | Real strict-TLS API → coordinator → execution Mac opened Arc; pairing/logout checked; user confirmed phone demo. Exact phone command and keyboard dictation not separately recorded.                                                                     |
| Dashboard schema and editor | Sol model + UI, `codex/dashboard-editor` | In progress                                | Create, rename, edit, delete and select dashboards; add/configure/reorder/remove built-in widgets; reload persists local layouts; bounds/import validation and mobile browser checks. Clock and notes work locally; integration cards state unconnected. |
| Local transcription adapter | Sol voice, `codex/local-voice`           | In progress                                | Explicit local executable/model; bounded WAV; cancellation/deadline/cleanup; useful CLI; no audio logging, retained recording, network fallback, or transcript-driven authorization.                                                                     |
| Physical-phone automation   | Coordinator, `codex/delivery-queue`      | In progress                                | Read-only prerequisite check plus repeatable Safari WebDriver flow; trust remains validated; separate physical, simulator and fake-driver evidence.                                                                                                      |

The dashboard editor first stores layout configuration in the browser. That does not establish household synchronization, profile isolation, or authenticated dashboard mutations. The voice adapter is a foundation for microphone capture and command routing; it is not full phone voice control.

## Next ready slices

1. **Stable development phone setup.** Replace repeatedly recreated test CAs with an explicitly initialized private development identity and bounded listener lifecycle. Preserve production identities. Test restart/pairing persistence and revocation without installing a new CA each run. Add public-CA-only profile export with fingerprint and teardown. Keep repeatable ephemeral CI identities separate.
2. **Chores model and chart.** Local dated assignments, completion/undo, household member labels, weekly completion chart and export. Define time-zone and recurrence semantics before reminders. Synthetic tests cover missed days, reset boundaries, duplicates and privacy. No calendar account needed.
3. **Weather adapter and widget.** Explicit configured place, read-only provider, bounded response, attribution, freshness and offline cache; no automatic precise-location collection. Provider access is opt-in and failure must not block the dashboard.
4. **Playlist widget.** Explicitly selected YouTube playlist IDs, title editing, user-started playback, unavailable/embedding-disabled state, constrained iframe origin and separate integration consent. No scraping, account-cookie reuse or arbitrary iframe URLs. Test with a fake player; real playback is a separate acceptance step.

## Dependent slices

| Feature                       | Implementable sequence                                                                                                                                                                                     | External acceptance gate                                                                                                                          |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full local voice              | WAV transcription → authenticated bounded audio upload → phone push-to-talk and stop/cancel → transient transcript review → granted intent routing → local spoken response → barge-in → optional wake word | Installed model, microphone consent, real speech/noise/latency checks. Voice identity may personalize; never grants access or tool permission.    |
| Shared dashboards and widgets | Versioned local layout/editor → household store/migrations → authenticated per-profile API → synchronization/conflicts → reusable widget registry and configuration UI → import/export                     | Two clients, read-only TV, private/shared data isolation, revocation and offline/reconnect acceptance.                                            |
| Google Calendar               | Provider contract and synthetic fixtures → OAuth connection/Keychain → selected-calendar read-only sync → cached agenda widget → explicit event create/edit later                                          | User-owned Google OAuth app/account consent. No live event writes until explicitly requested.                                                     |
| Gmail                         | Provider contract → least-scope OAuth/Keychain → bounded selected inbox metadata → private phone inbox widget → read selected content → drafts later                                                       | User OAuth consent; mail privacy scopes reviewed. Sending/deleting messages is separately authorized, never inferred from integration setup.      |
| Chores and routines           | Local chores/chart → shared profile API → recurrence/reminders → deterministic routine scheduler                                                                                                           | Time-zone/missed-run policy and no crash replay; notification destination consent where required.                                                 |
| Extensible widget ecosystem   | Built-in registry → typed configuration/data contracts → capability declarations → isolated external content → versioned third-party extension packaging                                                   | Untrusted widgets cannot execute arbitrary host code or inherit household credentials. Define sandbox and grant model before third-party loading. |

## Delivery rules

- Inspect current refs, instructions and open PRs. Verify write access on each working branch before substantial implementation. Preserve existing history and installed household services.
- Read this queue, active agent status and PR state before assigning work. One owner per file or worktree. Define interfaces before parallel dependent work.
- On completion: review the diff, run meaningful Node 24/Bun-managed checks, publish a small PR, record evidence and immediately select the next unblocked item. CI queueing is not a successful check. Never merge failed checks.
- Existing open PRs remain reviewable; merging the stack and deploying it require the applicable user authorization. A published PR is not a released feature.
- If a task blocks on a credential, consent or physical device, record the precise missing step once and continue another ready task. Do not repeatedly ask the same question.
- Maintain three concrete ready items. Replenish from the stated scope rather than creating unrelated work. Prefer usable vertical slices over placeholder-only PRs.
- All public fixtures are synthetic. Store OAuth credentials, phone identifiers, profile files, CA keys, model paths and household data outside the checkout. Exclude transcripts, recordings and private content from routine logs and failure artifacts.
- Background continuation is a scheduled local task, not a guarantee of uninterrupted execution. Checkpoint progress so host availability or usage limits do not lose work. Report only meaningful completions, blockers or decisions.
