# Household assistant delivery queue

This is the working queue for the household alpha. Each completion advances the next ready slice. CI delays, absent OAuth credentials, and attended phone steps block their own items, not independent implementation. Keep at most three implementation agents active, with one coordinating review. Keep changes independently reviewable and distinguish implementation, simulated tests, real hardware, and user-reported acceptance.

The product UI is native-first: SwiftUI on macOS and iPhone. Browser code remains a compatibility and protocol reference; new household interface work targets native views. Preserve the browser-v1 dashboard export path.

## Release priority and parallel work

The user explicitly corrected the workflow: new feature ideas must not displace the stability and shipping backlog. Freeze feature expansion until there is one reviewable, installable native candidate and an honest end-to-end acceptance record. Browser media and Apple Watch remain committed backlog requirements; complete only already-started review fixes before parking that feature work. PR count is not release progress.

Three independent agents are assigned, with root responsible for review, integration order, publishing and the release decision:

| Lane        | Owner and branch                                     | Concrete output                                                                                                                                                                             |
| ----------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Integration | native_credentials, `codex/native-service-staging`   | Build the agreed prebuilt Swift inspect/stage foundation from reviewed PR57. Publish only a verified immutable unselected release; preserve apps, plists, receipts and identities.          |
| Reliability | native_ios, `codex/release-ats-diagnostics`          | Adapt reviewed PR48 diagnostics onto PR56, preserving legacy trust and deterministic speech behavior. Return combined evidence for review.                                                  |
| Packaging   | native_weather, following `codex/packaged-launchers` | Verify copied PR57 launcher compatibility on the physical macOS 15.1 MacBook with a finite fake Node; preserve live services and report exact artifact versus substituted fixture evidence. |

The combined reliability branch is published as PR56 `a7f220b`, preserving PR51/52/54/55 histories from owner-tested PR50. Packaged launchers are based on PR53 `46051e0d`; all implementation branches were pushed before work. Feature source worktrees remain separate and quiescent. Integration uses owned synthetic source copies for validation and preserves reviewed feature commits. No agent may deploy over the household stack or broaden permissions to make a test pass.

The first three lane results are reviewed. [PR41](https://github.com/rdimascio/ellie/pull/41) packages a clean-source Mac candidate, [PR42](https://github.com/rdimascio/ellie/pull/42) adds real-process crash/reconnect/no-replay checks, and [PR43](https://github.com/rdimascio/ellie/pull/43) publishes the separately granted speech backend after timeout and late-cancellation fixes. The exact archived Mac app passed physical isolated dashboard creation, note editing and quit/relaunch persistence. The app-hosted iOS simulator passed an 11-second transcription through production Swift and Node HTTPS with synthetic audio. See [the candidate acceptance record](validation/2026-09-13-native-release-candidate.md). New PR native CI is still pending; none is deployed.

The combined native candidate is now published through PR50, and the owner reports successful native coordinator connection and an explicit Arc launch on the mini. That gate is accepted as owner-reported hardware validation. PR51 adds bounded iOS runner diagnostics, PR52 makes speech integration fixtures deterministic, and PR53 builds the offline service payload. The current blockers are remaining CI failures and checkout-free launch/install/upgrade/rollback. These tasks do not resume browser, Watch or provider expansion.

The already-started browser companion has been reviewed and published as [PR40](https://github.com/rdimascio/ellie/pull/40) at `8ade011`; feature expansion is parked. Root repeated its two loaded-extension synthetic browser scenarios and one shipping-policy check. Its GitHub checks passed; no installed native/browser/Watch functionality is claimed. The three release lanes take priority over its follow-up native bridge.

The first usable release target is native Mac dashboards and explicit cross-Mac control in one installed candidate. Complete iPhone signing/enrollment and real microphone acceptance as separate subsequent gates. Do not add browser/Watch/provider features to the release blocker list merely because they are newer requests. Queue them after the stable base; reassign a blocked lane to the next release blocker, not the newest idea.

The user has made **in-page media control** a core requirement: browse down a catalogue, move across a row, open a title, play and pause on Netflix, YouTube, YouTube TV and Disney+. Opening a URL does not satisfy it. The [browser media plan](browser-media-control.md) adds WebMCP discovery and a companion fallback with a native SwiftUI remote. Actual service and phone-to-browser acceptance remain pending.

**Apple Watch is included:** surface the current Ellie media session with player controls while it plays. Plan a native watch remote and evaluate Smart Stack Live Activities. Built-in Now Playing automatically chooses watch/iPhone audio sources; arbitrary remote Mac playback integration is unverified. Supported automatic presentation and locked-phone limits remain research prerequisites after the stable base. No Watch app is installed or validated.

## Current delivery

| Item                            | Owner / branch                               | State                                                 | Acceptance                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------- | -------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Granted phone app opening       | PR #19, `codex/phone-control-demo`           | Implemented; CI passed; user reports phone demo works | Real strict-TLS API → coordinator → execution Mac opened Arc; pairing/logout checked. Exact phone command and dictation not separately recorded.                                                                                                                                                                                                                                          |
| Browser dashboard schema/editor | PR #22, `codex/dashboard-editor`             | Implemented; CI passed; compatibility surface         | Local layout editing and validated import/export; 15 Chromium and 5 WebKit dashboard checks. Clock and notes work; provider cards are unconnected.                                                                                                                                                                                                                                        |
| Native SwiftUI Mac app          | PR #23, `codex/native-dashboard`             | Implemented; CI passed                                | Real Dock app, native sidebar/toolbars/menus/sheets/file dialogs; local clock, notes and dashboard persistence. Physical mini UI create/rename/edit/restart/export/import-cancel passed. Nine synthetic XCTest cases passed on MacBook Xcode.                                                                                                                                             |
| Native chores and weekly chart  | PR #24, `codex/native-chores`                | Implemented; CI passed                                | One-off dated assignments, completion/undo and local weekly chart. Real mini UI add/complete/undo/restart passed. Fifteen synthetic Swift tests passed on MacBook Xcode. Household synchronization and recurring chores remain pending.                                                                                                                                                   |
| Native coordinator Devices      | PR #25, `codex/native-connection`            | Implemented; CI passed; LAN consent required          | Read-only pinned connection with bounded retries, cancellation and private existing identity access. Forty-four Swift tests passed, including five actual URLSession tests using synthetic loopback HTTPS. macOS blocked native LAN access; CLI doctor passes.                                                                                                                            |
| Native granted app controls     | PR #26, `codex/native-actions`               | Implemented; exact-head GitHub checks passed          | Explicit target and app, one request at a time, bounded transport and truthful unknown outcomes after cancellation. Fifty-five Swift tests passed; real mini UI used synthetic clients. No new household action acceptance.                                                                                                                                                               |
| Native current weather          | PR #27, `codex/native-weather`               | Implemented; all GitHub checks passed                 | Twenty Swift tests passed. Real mini UI fetched a public-city forecast over HTTPS, refreshed, preserved the cache across restart, rendered the night symbol and cleared state on disable. Place selection is explicit and opt-in.                                                                                                                                                         |
| Native iPhone dashboard         | PR #28, `codex/native-ios`                   | Implemented; all GitHub checks passed                 | iOS simulator build and one expanded UI workflow passed: note edit/relaunch persistence, dashboard create/rename/delete and return navigation. Ten shared Swift tests passed. No physical iPhone, pairing or microphone acceptance.                                                                                                                                                       |
| Local transcription adapter     | PR #21, `codex/local-voice`                  | Implemented; CI passed                                | Bounded local whisper.cpp WAV transcription, cancellation/deadline/cleanup; no audio logging or cloud fallback. Real tiny.en inference and cancellation passed with synthetic audio; microphone remains untested.                                                                                                                                                                         |
| Native enrollment foundation    | PR #29, `codex/native-enrollment`            | Implemented; all GitHub checks passed                 | 180 Node tests passed, one skip. Canonical capacity-tested QR, separate native bearer, private persistence, restart/revocation and lost-response recovery. Swift camera, pinned transport and Keychain client are implemented in PR35.                                                                                                                                                    |
| Native local push-to-talk       | PR #30, `codex/native-push-to-talk`          | Implemented; all GitHub checks passed                 | 59 Swift tests plus final four-test voice rerun; 169 Node tests passed, one skip. Real mini Whisper + synthetic audio reached native transcript review. Unsupported edit blocked; review never dispatched. Microphone/LAN voice acceptance pending.                                                                                                                                       |
| Native selected playlists       | PR #31, `codex/native-playlists`             | Implemented; all GitHub checks passed                 | 165 Node tests passed, one skip; six focused Swift playlist tests. Actual mini YouTube playback advanced to25s, pause/resume, close/restart and fresh playback passed. WebKit grammar and trusted Blob resource regressions use the real engine.                                                                                                                                          |
| Native listener contract        | PR #32, `codex/native-listener-contract`     | Implemented; all GitHub checks passed                 | Dedicated native OpenAPI and shared QR fixtures (6 valid, 34 invalid) consumed by Swift; Node real-parser regressions passed. No hardware effects.                                                                                                                                                                                                                                        |
| Combined native integration     | PR #33, `codex/native-integration`           | Implemented; all GitHub checks passed                 | 190 Node tests/one skip, 83 Swift tests, generic iOS build and simulator UI rerun passed. Physical mini dashboard/notes/chores/chart/restart/Devices/setup and separate accessible chore actions passed using synthetic state. Full keyboard activation remains unverified.                                                                                                               |
| Native imported agenda          | PR #34, `codex/native-calendar`              | Implemented; all GitHub checks passed                 | 162 Node tests/one skip, full Swift27 plus final focused9 passed. Physical mini import, display-zone date ordering, expiry filtering, restart, and cache clearing passed with synthetic content. Live Calendar/OAuth not implemented.                                                                                                                                                     |
| Native iPhone enrollment client | PR #35, `codex/ios-enrollment`               | Implemented; all GitHub checks passed                 | 182 Node tests/one skip; final Swift29; two iOS UI and five app-hosted HTTPS/Keychain tests. CI recovery head5410c48 passed all checks; latestbdcd678 adds complete ASCII validation. Physical enrollment remains pending.                                                                                                                                                                |
| Native iPhone app controls      | PR #36, `codex/native-phone-controls`        | Implemented; all GitHub checks passed                 | Node198/one skip; Swift35 before inherited parser test; generic iOS build and synthetic native UI workflow passed. Actual Simulator Swift transport → production Node HTTPS listener produced exactly one simulated Safari dispatch; grants/logout/revocation, Keychain and ATS checks passed. Service bridge lifecycle validated synthetically. No physical native phone-to-Mac command. |
| Native Mac pairing management   | PR #37, `codex/native-pairing-management`    | Implemented; all GitHub checks passed                 | Node190/one skip, final Swift91, actual loopback TLS MIME rejection, immediate cancellation, canonical delegated authority, and Vision decoding of maximum2300 QR passed. Physical mini native GUI passed with synthetic16 Macs/18 iPhones. Optical camera/LAN enrollment pending.                                                                                                        |
| Native household integration    | PR #38, `codex/native-household-integration` | Published; all GitHub checks passed                   | Final Swift 129 passed; Node 208/one skip; two iOS UI, one targeted playlist-editor UI and five app-hosted HTTPS/Keychain tests passed. Physical mini synthetic agenda/title/chores/chart/Devices/relaunch passed. Review fixed imported playlist settings lost by iPhone editing. No live deployment.                                                                                    |
| Household-state backend         | PR #39, `codex/household-state`              | Published; all GitHub checks passed                   | Full Node 233 passed/one skip; independent storage14, actual Foundation shared-fixture1 and final contract10 passed. Real local HTTPS runtime/controller CLI handlers/two native clients verified conflict, lost-response GET recovery, private isolation, durable restart and revocation with zero desktop actions. Native sync UI follows separately.                                   |
| Physical-phone automation       | PR #20, `codex/delivery-queue`               | Runner implemented; device acceptance partial         | Safari WebDriver prerequisite/fake-driver checks; actual Mirroring connection and Settings navigation verified. User removed expired demo profile. No phone action remains pending; certificate installation/trust and physical WebDriver remain unverified.                                                                                                                              |

Native dashboards and chores currently save on one device. PR38 combines Mac household widgets, agenda, Devices and pairing management with iPhone enrollment and app controls in one isolated SwiftUI integration candidate. It is published, not deployed. PR39 adds the explicit data-authority and durable-state backend. Native synchronization screens are implemented in PR45 and combined in PR46; person profiles, physical synchronization acceptance, real microphone acceptance and signed iPhone distribution remain pending. Calendar imports a private offline snapshot; Google account access is not implemented.

## Next ready slices

The **release lanes above override this feature queue**. The following implementation history remains for review and sequencing, not permission to resume feature expansion.

The completed native feature sources now have separate preserved histories: [PR43](https://github.com/rdimascio/ellie/pull/43) (`1ecad24`) for independently granted speech, [PR44](https://github.com/rdimascio/ellie/pull/44) (`ac26921`) for iPhone push-to-talk, and [PR45](https://github.com/rdimascio/ellie/pull/45) (`5deac30`) for explicit dashboard synchronization. [PR46](https://github.com/rdimascio/ellie/pull/46) (`1f975a5`) combines their original histories and the app-hosted Swift-to-Node integration gate. These remain unmerged and undeployed. Their source and tests are ready for release integration; physical phone and microphone acceptance remain separate gates.

The next three release tasks are:

1. **Complete CI reliability fixes.** The process-fixture readiness fix is published in PR54; finish the dashboard late-read regression review. Combine compatible fixes with PR51/52 in a fresh integration branch, preserving histories, and run the combined gate. Inspect actual failures without blind retries or relaxed acceptance counts.
2. **Packaged launchers.** Build stable-identity role launchers over reviewed PR53. Validate every declared payload file and reject extras, links and writable installed content; use a single helper resolver for execution, Keychain and diagnostics. Test actual shipping argument construction plus an explicitly separate test build using owned synthetic state. Ad-hoc tests do not prove permission or Keychain consent carryover.
3. **Transactional installation next.** After launchers are reviewed, implement private immutable release staging, explicit role registration and stopped-role upgrade/rollback, then validate login/restarts/network loss/sleep-wake without action replay. Preserve existing identities and consent. Live deployment and signed distribution remain separate gates.

All new working branches must be pushed before implementation. Root reviews the resulting code and evidence before publishing implementation PRs. The browser companion stays parked while these release gates advance.

Stable private development identities still matter for repeatable browser compatibility tests. Preserve existing production identities, avoid regenerated certificate profiles each run, and keep ephemeral CI identities separate.

## Dependent slices

| Feature                       | Implementable sequence                                                                                                                                                                                                                               | External acceptance gate                                                                                                                          |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full local voice              | WAV transcription → native microphone capture → authenticated bounded audio upload where needed → native push-to-talk and stop/cancel → transient transcript review → granted intent routing → local spoken response → barge-in → optional wake word | Installed model, microphone consent, real speech/noise/latency checks. Voice identity may personalize; never grants access or tool permission.    |
| Shared dashboards and widgets | Native versioned local layout/editor → household store/migrations → authenticated per-profile API → synchronization/conflicts → reusable widget registry and configuration UI → import/export                                                        | Two clients, read-only TV, private/shared data isolation, revocation and offline/reconnect acceptance.                                            |
| Google Calendar               | Provider contract and synthetic fixtures → OAuth connection/Keychain → selected-calendar read-only sync → cached agenda widget → explicit event create/edit later                                                                                    | User-owned Google OAuth app/account consent. No live event writes until explicitly requested.                                                     |
| Gmail                         | Provider contract → least-scope OAuth/Keychain → bounded selected inbox metadata → private phone inbox widget → read selected content → drafts later                                                                                                 | User OAuth consent; mail privacy scopes reviewed. Sending/deleting messages is separately authorized, never inferred from integration setup.      |
| Chores and routines           | Local chores/chart → shared profile API → recurrence/reminders → deterministic routine scheduler                                                                                                                                                     | Time-zone/missed-run policy and no crash replay; notification destination consent where required.                                                 |
| Extensible widget ecosystem   | Built-in registry → typed configuration/data contracts → capability declarations → isolated external content → versioned third-party extension packaging                                                                                             | Untrusted widgets cannot execute arbitrary host code or inherit household credentials. Define sandbox and grant model before third-party loading. |

## Delivery rules

- Inspect current refs, instructions and open PRs. Verify write access on each working branch before substantial implementation. Preserve existing history and installed household services.
- Read this queue, active agent status and PR state before assigning work. One owner per file or worktree. Define interfaces before parallel dependent work.
- On completion: review the diff, run meaningful Node 24/Bun-managed checks, publish a small PR, record evidence and immediately select the next unblocked item. CI queueing is not a successful check. Never merge failed checks.
- Existing open PRs remain reviewable; merging the stack and deploying it require the applicable user authorization. A published PR is not a released feature.
- If a task blocks on a credential, consent or physical device, record the precise missing step once and continue another ready task. Do not repeatedly ask the same question.
- Maintain three concrete ready items. Replenish from the stated scope rather than creating unrelated work. Prefer usable vertical slices over placeholder-only PRs.
- All public fixtures are synthetic. Store OAuth credentials, phone identifiers, profile files, CA keys, model paths and household data outside the checkout. Exclude transcripts, recordings and private content from routine logs and failure artifacts.
- Background continuation is a scheduled local task, not a guarantee of uninterrupted execution. Checkpoint progress so host availability or usage limits do not lose work. Report only meaningful completions, blockers or decisions.

## Latest checkpoint

- Reviewed and published [PR58](https://github.com/rdimascio/ellie/pull/58), `5b435d941bfb2f82852f2f9fcfa8f38c4c0b2e3e`, based on PR56 and preserving original PR48 through a merge. Its three-file diagnostics integration retains default no-op product behavior, fixed error categories and unchanged legacy trust, timeouts and speech semantics. Synthetic enrollment/control request and response counts are exactly session 1, inventory 2, command 1, logout 1 and unexpected 0; speech has its own unchanged turn checks. Full Node: 252 passed, one skip; Swift: 158 passed; owned app-hosted ATS gate passed and simulator/remote cleanup completed. The retained summary does not independently establish an XCTest method count, so no count is claimed for this run. Prior intermittent recovery `unavailable` remains unexplained; this change makes a recurrence diagnosable. No deployment or new preview occurred.
- Reviewed and published [PR57](https://github.com/rdimascio/ellie/pull/57), `496237bdd5ae1cb40366ac891604db6e293f33e6`, based on PR53. Root verified the exact head/base, 13-file scope and clean tree after all launcher review fixes. The common helper resolver serves Keychain, execution and diagnostics; bundle identities/icons and fixed roles are preserved. Full check before the final small enumeration/test refinements: 218 passed, one platform skip. Final focused config/launcher/payload cases: 16 passed, including same-length hash tamper. A copied full prepared payload with only Node replaced by a finite script passed both launchers' complete-tree verification and fixed arguments/environment; the actual CLI was not run. The separate final clean-commit artifact is `EllieServices-0.1.0-dev-496237bd-macos-arm64.zip`, SHA256 `4540d2b107d602cc59ba0315561fd5814ed476e3fa5d6d0f7b0b21a923127af6`. Root independently verified its checksum and both launcher signatures. Manifest source is the published head, with 409 files and 29 components. No launchd, Keychain, permission or household-state mutation occurred. Native service staging starts from this exact reviewed head, with its working branch pushed before implementation.
- Completed CI classification: PR55 run `34780287544` passed the standard iOS runner and failed only old speech-fixture postconditions (five uploads, three starts); PR55 push run `34780277511` failed the old `ready`-before-SIGTERM fixture before native builds. Root verified the exact failed head `2cbf6cc` still contains that old ordering. PR56 includes both relevant fixes; there is no evidence of a residual process race in PR56 from those runs. PR54 run `34780121747` reached app-hosted ATS testing after simulator boot and failed pinned session recovery with `unavailable`; its cause remains unknown. The reliability agent is adapting already-reviewed PR48 coarse diagnostics onto PR56 while preserving current legacy trust and deterministic speech behavior.
- The next installer boundary is a prebuilt macOS 14 Swift executable, avoiding an existing destination Node/Bun/checkout. Its first implementation slice will inspect and stage a verified immutable, unselected release using descriptor-relative no-follow filesystem operations; role publication and the durable stopped-role upgrade/rollback journal follow in a separate review. It will derive release IDs from existing validated manifest fields and use installer-controlled signing policy. Ad-hoc signature checks establish development integrity, not Developer ID authenticity. No installer action is authorized against the live household installation.
- Root reviewed and published [PR56](https://github.com/rdimascio/ellie/pull/56), `a7f220b0745caad143886095776fc6f0b8cc21c5`, based on PR50. Seven original/merge commits preserve PR51/52/54/55, with eight expected files and no merge conflicts. Root independently compared every combined source area with its reviewed branch. Combined Node: 252 passed, one skip; exact-source desktop Swift: 158 passed; app-hosted iOS: 11 passed; standard iOS UI: two passed. Owned simulators, fake workers and remote source were cleaned. Remote head/base/files and clean local tree are verified. TypeScript CI has passed; native/web checks were still running at this checkpoint. No new preview, household action, microphone acceptance or deployment occurred.
- PR53 at `46051e0d` now has all exact-head GitHub checks passing on all three inspected runs, including native CI. PR54 has one successful native run and one failed native run; PR55 has two failed native runs. The reliability agent is inspecting those completed failures to distinguish known absent fixes from simulator startup or a new regression. Passing combined local tests do not make those failures resolved.
- Packaged-launcher review found four actionable gaps: empty directory traversal was unbounded and accepted undeclared directories; revision validation accepted a terminal newline; required runtime paths lacked semantic executable/data mode checks; extracted app signatures were not verified after ZIP round-trip. Root also found the new workspace dependency missing from the lockfile and contradictory helper-resolution documentation. The packaging agent is correcting these before publication. Installer implementation remains gated on that reviewed launcher boundary.
- Root reviewed and published [PR55](https://github.com/rdimascio/ellie/pull/55), `2cbf6cc614965b977c09d12bc6d18577a7de7db2`, based on PR51. The dashboard regression now gates request start and awaits the exact canceled task returned by a small internal lifecycle seam. Existing cancellation/generation behavior is unchanged. Disabling generation rejection in an owned negative-control copy fails both assertions; the exact positive and full Swift 158 tests pass. The integration agent is incorporating this reviewed head into the combined reliability branch and will run combined native gates. The reliability agent is now independently reviewing the quiescent launcher diff. Launcher focused tests pass 15/15 and a clean synthetic full payload builds; root review/publication remain pending.
- Reviewed and published [PR54](https://github.com/rdimascio/ellie/pull/54), `e1868a5a9a2438c15476ba925dd33f72b78364e0`, based on PR52. The one-line test-fixture fix installs its SIGTERM listener before announcing readiness. Root verified the one-file/one-commit scope and base/head. The completed PR52 TypeScript job `103784629893` log confirms this same pre-fix race: `Fixture exited before term` after 204ms, with 247 passed, one failed and two platform skips. No blind rerun was triggered. The integration agent is now combining PR51/54 in a new isolated branch, awaiting the separately reviewed dashboard fix before final publication.
- Reviewed and published [PR51](https://github.com/rdimascio/ellie/pull/51), `db6c4b207fd4b915a4ba9abf400a6d5707534640`, based on `codex/native-legacy-trust`. Its three-file change retains bounded failure-stage diagnostics before Xcode creates an xcresult. Full Node gate: 252 passed, one skip; focused diagnostics: three passed; owned MacBook iOS simulator: two UI tests passed with shutdown/delete complete. This diagnoses future failures; it does not resolve the earlier 120-second boot-readiness timeout. New PR51 CI failures occurred before the iOS runner: dashboard background-test ordering in run `34779429528` and a shutdown-fixture readiness race in `34779443920`.
- Reviewed and published [PR52](https://github.com/rdimascio/ellie/pull/52), `e1cb8a19129119eca9ec1b274802ec4cc44b7d92`, based on `codex/native-legacy-trust`. Two test/harness files now use separately authenticated synthetic speech start/settlement acknowledgements. Production TLS, timeouts and authority remain unchanged. Owned MacBook app-hosted iOS tests: 11 passed with exactly five uploads and start/exit marker progression `[0, 0, 2, 3, 4]`; no extra app action. Full Node gate: 249 passed, one skip. Fresh CI is not green: native jobs remain in progress and TypeScript job `103784629893` failed; its confirmed fixture race is corrected in PR54 above.
- Reviewed and published [PR53](https://github.com/rdimascio/ellie/pull/53), `46051e0d5d896070a05f8501a86bbd1b6801b265`, based on distribution plan `9b410b8`. The offline builder captures clean committed source, supplied-checksum-verified official Node 24, production dependency/license closure and a macOS 14 helper in an exact file/hash/mode manifest. Seven final focused cases, lint, format and types passed; an earlier full Node gate passed 214 with one skip before the final small test/environment refinements. Copied production runtime with synthetic TLS/state and a fake executor completed one app-open and restarted the node without replay. A prepared snapshot's helper/CLI ran on the physical macOS 15.1 MacBook without Accessibility consent. The final clean-commit ZIP is `EllieServices-0.1.0-dev-46051e0d-macos-arm64.zip`, SHA256 `7c9b4613eec6b2f27a783798204fa8a80cf48e8e655ce22df5a3447792f563fb`; root independently verified its hash, helper signature and minimum OS. This exact final ZIP was not separately exercised on the MacBook. It is preparation material, not an installer or immutable installed release. TypeScript/web CI passed on inspected runs; native checks remain pending.
- The next three lanes above are underway. Root approved the one-line process fixture fix after inspection: register the SIGTERM listener before announcing readiness. Ten focused repeats, both recovery cases and full Node 249 passed/one skip passed. Dashboard test review found that replacing its start delay alone still left completion unproven; the agent is adding deterministic completion and a negative control before publication. Packaged launcher work remains unreviewed and undeployed.
- Current checkpoints above supersede historical pending-test and agent-assignment statements below. No live household deployment or open-stack merge occurred.
- Owner accepted the corrected `e23db80d` native Mac preview on 2026-09-13: coordinator connection succeeded and an explicit Arc launch succeeded on the paired mini. This is owner-reported physical two-Mac acceptance, distinct from the prior synthetic TLS tests. No repeat of this same acceptance step is needed. The owner also reported a disconnected mini in the device list. Current coordinator inventory retains registrations for its process lifetime; the native view marks them offline after 60 seconds and disables app dispatch. Whether the owner sees a separate stale identity or the successful target itself turning offline is awaiting clarification. Do not delete/revoke registrations or change pairing to tidy the list. Readable device names and clear online/offline grouping belong in the usability backlog.
- Owner testing PR49 exposed a native trust failure against the original CN-only coordinator certificate. [PR50](https://github.com/rdimascio/ellie/pull/50), `e23db80dcaeb8d27adb269c48a41d4968ad550bd`, fixes compatibility with that exact pinned legacy profile; existing identities and credentials are preserved. The current-generator HTTPS regression fails on pristine `0838a00c` and passes with the patch. All 158 Swift tests and the full Node gate (249 passed, one skip) pass. An isolated app bundle on macOS 15.1 with unchanged ATS policy successfully read one synthetic node and rejected a wrong legacy pin with zero extra requests. The clean-source ZIP has SHA256 `2b33e46317c139010d39375485a345d2d33bf05ee980f810fee667a0792ec4b4`; it was copied and extracted to a fresh owner preview folder, with checksum, signature, plist and provenance verified. Owner retry of native Coordinator connection and one explicit mini app-open passed; see the [owner acceptance record](validation/2026-09-13-native-cross-mac-owner-acceptance.md). Do not replace the old preview in place, re-pair, rotate certificates or restart live services. The full validation scope is recorded in PR50's dated compatibility document and description.
- PR49's two completed native CI failures are separate iOS speech-fixture failures: run `34774521436` passed 22/23 app-hosted cases but the delayed speech test returned `busy`; run `34774522997` passed all 23 XCTest cases, then failed the harness postcondition with five uploads but two fixture process starts. Desktop stages passed in both runs. The retry `34776129034` remained in progress at the last inspection. Do not call these resolved by PR50; deterministic speech-fixture settlement/counting is the next reliability task.
- Root reviewed and published [PR49](https://github.com/rdimascio/ellie/pull/49), combined native candidate `0838a00c99a17c7517c5d6c858e380f3c17e3dbc`, preserving PR46/41/42 histories. Full Node gate: 249 passed, one platform skip; Swift: 152 passed; app-hosted iOS: 11 passed. The ZIP checksum is `b184835283bcddaef0bb8ae0b547f872cb16b74c65fb8a1d0c36c91b097adeee`. The exact ZIP and extracted app were copied to the owner's MacBook preview folder and passed checksum, plist and signature verification there. The owner can now test the native dashboard and Devices flow. This artifact's physical UI/LAN command acceptance is pending; do not interfere with the owner's preview or restart live services. GitHub CI for the newly published candidate remains pending.
- Release review published PR41 `b48bed1`, PR42 `b4945a3` and PR43 `1ecad24`. Their TypeScript, web and security checks have passed on completed runs; native CI remains pending. PR39 also has one failed native run (`34769051186`) among passing runs: enrollment recovery returned a fixed unavailable error after three seconds. The retained artifact does not expose its cause. The current owned iOS runner passed, but that does not prove the CI failure was a tooling flake. A separate reliability task will add sanitized stage diagnostics without retries or weaker TLS.
- The clean Mac ZIP is `Ellie-0.1.0-dev-b48bed18.zip`, SHA-256 `5c349e5bdcdbe8e71e2b3b7f2da625d762afd1c90a88dab776936e8ede8dbdd1`. Its source includes committed build/package scripts; the older `88d15685` artifact is superseded. Archive extraction, signature, plist, provenance and physical isolated UI persistence passed. It remains an ad-hoc development app requiring existing services; checkout-free distribution and attended native LAN command acceptance remain open.
- Integration snapshot matches backend `1ecad24` across all 20 backend files and the reviewed iPhone speech/sync sources. Eleven app-hosted iOS tests passed, including an 11-second result, exactly five uploads and zero additional app-open dispatch. Earlier combined Swift suite: 152 passed. Latest backend full Node check: 247 passed, one platform skip. PR42 full check: 235 passed, one platform skip; two focused process cases pass, including bounded TERM-to-KILL teardown before deleting owned state. None of these synthetic speech/process checks imply physical microphone or sleep/wake acceptance.
- PR39 at `9e75923` is published on `codex/household-state`, based on PR38. It adds explicit native data grants, separate hardened stores, conditional shared/device-private documents, controller CLI and exact native/coordinator contracts. Full Node check passed 233 with one skip, independent store14 passed, shared Foundation fixture1 passed and final contract10 passed. The end-to-end test uses real production runtime/controller CLI handlers and two local HTTPS clients with synthetic credentials; it drops a committed response, recovers by GET, rejects stale writes, checks private isolation/restart/revocation and proves no desktop action. It does not invoke installed CLI Keychain or physical phone. Earlier GitHub runs passed; the later native failure is tracked above.
- Coordinating review fixed queued session expiry, pre-I/O capacity rejection without poisoning, closed-store listing, Swift/TypeScript schema mismatches, request channels and response classifications. OpenAPI now carries exact structures, revision/ETag and UTF-16 constraints. Representative timezone fixtures pass; exhaustive Foundation/Node alias equivalence is not claimed. One intentional control-character regex lint finding was narrowly documented before the complete passing check.
- PR38 at `88d1568` and prior PR20 at `1db029e` now have all exact-head GitHub checks passing. PR39 remains unmerged and undeployed. Existing household installations and credentials are preserved.
- The three active next worktrees above were each created and successfully pushed from `9e75923` before implementation. Their agents must return quiescent diffs for coordinating review before any commit/push. Keep file ownership separate and integrate compatible histories only after each slice passes review. Native microphone, physical phone, native LAN commands and Mac sync enrollment remain separate acceptance/design gates.

- PR38 at `88d1568` preserves the PR37, PR36 and PR34 histories via local merge commits `26bb9b1` and `ee4ef79`. Combined Node checks passed 208 with one skip; final full Swift passed 129. Two standard iPhone UI tests and one isolated production playlist-editor regression passed. Five app-hosted ATS/Keychain tests passed before the editor-only fix with exactly two synthetic inventory reads and one simulated app-open; unchanged transport was not retested. All exact-head GitHub checks passed.
- Coordinating review caught and fixed iPhone edits erasing imported playlist configuration. The shared helper preserves schema-valid settings, clears an explicitly blank clock zone and rejects unknown fields. Two stale Mac agenda descriptions were corrected. The native app and dated integration validation documents contain exact run instructions and limitations.
- Physical mini native UI at `ee4ef79`, with private synthetic state and an injected coordinator loader that cannot access the household, passed agenda display/title editing, chore completion/chart, Devices navigation and actual process-relaunch persistence. The first handmade chore fixture had a non-UUID ID and was correctly rejected; correcting the fixture enabled the successful test. This hardware UI copy predates the iPhone helper and gallery wording fix; the final source passed Swift and Simulator tests. No live service, Keychain, LAN, microphone or camera was used.
- PR35 at `bdcd678`, PR36 at `c02803e`, PR37 at `919d092` and the prior PR20 head `ce1dd07` now have all exact-head GitHub checks passing. Their earlier pending-CI records are superseded. PR35's cancelled CI job remains distinct from the independently reproduced local Process.waitUntilExit bug; the latter was fixed with bounded process cleanup.

- Earlier enrollment snapshot, superseded by the fixes above: PR #35 at `9ef40a4` adds explicit QR scanning and origin/grant confirmation, an atomic device-only Keychain envelope, pinned TLS 1.2+ transport and durable read-only recovery without pairing replay. Full Node checks passed 182 with one skip; 28 macOS Swift tests passed. Two simulator UI tests passed after retained selector/navigation/input failures were corrected. A separate five-test app-hosted simulator suite passed real pinned HTTPS, wrong-pin/hostname rejection and isolated Keychain persistence/promotion/removal with sentinel preservation. The harness verifies exactly one valid native request and the built app policy; only the local networking exception is present, and fixture settings stay out of the app. CI now runs this bounded test with failure-result retention. GitHub checks are pending. Physical phone camera/Keychain/LAN/signing and native command dispatch remain separate gates. No live service or owner credential was used.
- Native listener contract PR #32 at `d0f0e7f` publishes the dedicated listener OpenAPI and a generated cross-language fixture suite (6 valid and 34 invalid QR envelopes). Full Node checks passed182 with one skip; final focused parser/contract tests passed5. The fixtures cover canonical Unicode/escaping, IPv4/IPv6/IDNA origins, legacy numeric host rejection, DNS names that resemble hexadecimal, integer bounds and nonzero base64 padding bits. All exact-head GitHub checks passed. No hardware state changed.
- Combined native integration PR #33 at `68730d8` preserves the reviewed native histories in one isolated candidate. Node checks passed190 with one skip, full Swift83 passed, generic iOS18.2 build passed and the unchanged simulator dashboard workflow passed after one retained typing flake. Physical mini UI confirmed dashboard creation/rename, note and completed-chore restart persistence, weekly chart feedback, Devices window routing and playlist setup/cancel with private synthetic state. Final chore accessibility changes expose separate labelled actions; actual label-based completion and delete-confirmation cancel passed. The final app compiled and its signature verified. Full keyboard activation was not accepted on this host; standard system focus preferences were preserved. All exact-head GitHub checks passed. This candidate is not deployed.
- Read-only native agenda PR #34 at `b929581` adds explicit snapshot import and a bounded private cache. Node checks passed162 with one skip; post-review full Swift27 and final focused Calendar9 passed. Physical mini UI caught and then verified the fix for timed/all-day ordering in the display time zone, plus expired-event filtering, localized dates, persistence and clearing. Only synthetic content was imported. All exact-head GitHub checks passed. Google Calendar, OAuth, synchronization and event writes remain unimplemented.
- PRs #19–#25 and #27–#28 are published with passing checks. Existing dated records distinguish user-reported phone demo acceptance, simulator iPhone dashboards, real native dashboard/chores/weather UI and synthetic coordinator transports. PR26 now has passing exact-head checks at `a21393d`; its earlier stuck runner record is obsolete and should not be retried.
- Native enrollment PR #29 at `58e96d1` implements the separate canonical QR/server/session/CLI foundation on PR28. Full Node 24/Bun checks: 180 passed, one platform skip. Tests include actual QR level-M capacity, exact request channels, lost-pair-response candidate authentication, private-file pair/reopen, revoke/reopen and fail-closed persistence. No physical phone, camera, real Keychain credential or installed coordinator was involved. The dedicated listener OpenAPI now ships in PR32; the Swift enrollment client now ships in PR35. All exact-head GitHub checks passed.
- Native voice PR #30 at `1713dda` builds on PR26 and includes PR21's existing speech adapter. Full Swift suite passed59; final exact-source voice rerun passed4; Node checks passed169, one skip. Final release app built and verified on the mini. Real whisper-cpp1.9.2 with checksum-verified tiny.en transcribed synthetic “Open Safari.” through source and bundled bridges, and the production SwiftUI/transcriber showed editable review using an injected synthetic recorder and coordinator. Unsupported text was blocked; accepting review changed only the app picker; discard/cancel returned to idle. No microphone or command was used. All exact-head GitHub checks passed.
- The unpublished voice draft contained unsafe cleanup: two early focused tests attempted recursive removal of the MacBook user temporary root. Read-only inspection showed the root still present with private permissions and hundreds of entries; impact on individual temporary entries is unknown. Review stopped that path and replaced cleanup with recorder-owned exact files and empty-directory removal. Later regressions cover cancellation, EOF/size/deadline bounds and owned process groups. The dated voice hardware record documents the incident; do not claim the original tests were harmless or that all temporary data remained unchanged.
- Native playlists PR #31 at `1fdc180` builds on PR27. Full Node checks passed165, one skip; initial24 Swift cases and final6 focused playlist cases passed. Final release app built and signature verified. Physical mini testing with an official public playlist confirmed actual media progress from3 to20s, pause at25s, resume, close, restart persistence with no automatic playback, and fresh playback from0. Actual WebKit tests caught unsupported regex grammar, exact `/embed` navigation and blocked trusted Blob media. No private playlist/account or household state was used. All exact-head GitHub checks passed.
- A public local Whisper installation/model was added for repeatable physical validation. It did not start a service or change voice preferences. Automated logs exclude recordings/transcripts/private model paths; the explicit synthetic hardware runner is in PR30. Microphone, iPhone camera, native LAN commands, signed distribution, synchronization and Google account acceptance remain separate gates.
- Phone cleanup remains complete; no phone prompt is pending. Native Local Network consent is already recorded as pending; no privacy setting was changed or bypassed. Preserve installed mini `/Users/ryan/ellie`, MacBook `/Users/ryan/ellie-test`, `~/.ellie`, Keychain and all history. No open PR stack was merged or deployed.
- Next work is the three concrete ready slices above, with one owner per isolated worktree and a verified branch push before implementation. Keep provider/account work synthetic until the needed consent exists.
