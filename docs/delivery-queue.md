# Household assistant delivery queue

This is the working queue for the household alpha. Each completion advances the next ready slice. CI delays, absent OAuth credentials, and attended phone steps block their own items, not independent implementation. Keep at most three implementation agents active, with one coordinating review. Keep changes independently reviewable and distinguish implementation, simulated tests, real hardware, and user-reported acceptance.

The product UI is native-first: SwiftUI on macOS and iPhone. Browser code remains a compatibility and protocol reference; new household interface work targets native views. Preserve the browser-v1 dashboard export path.

## Current delivery

| Item                            | Owner / branch                      | State                                                                              | Acceptance                                                                                                                                                                                                                                                     |
| ------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Granted phone app opening       | PR #19, `codex/phone-control-demo`  | Implemented; CI passed; user reports phone demo works                              | Real strict-TLS API → coordinator → execution Mac opened Arc; pairing/logout checked. Exact phone command and dictation not separately recorded.                                                                                                               |
| Browser dashboard schema/editor | PR #22, `codex/dashboard-editor`    | Implemented; CI passed; compatibility surface                                      | Local layout editing and validated import/export; 15 Chromium and 5 WebKit dashboard checks. Clock and notes work; provider cards are unconnected.                                                                                                             |
| Native SwiftUI Mac app          | PR #23, `codex/native-dashboard`    | Implemented; CI passed                                                             | Real Dock app, native sidebar/toolbars/menus/sheets/file dialogs; local clock, notes and dashboard persistence. Physical mini UI create/rename/edit/restart/export/import-cancel passed. Nine synthetic XCTest cases passed on MacBook Xcode.                  |
| Native chores and weekly chart  | PR #24, `codex/native-chores`       | Implemented; CI passed                                                             | One-off dated assignments, completion/undo and local weekly chart. Real mini UI add/complete/undo/restart passed. Fifteen synthetic Swift tests passed on MacBook Xcode. Household synchronization and recurring chores remain pending.                        |
| Native coordinator Devices      | PR #25, `codex/native-connection`   | Implemented; CI passed; LAN consent required                                       | Read-only pinned connection with bounded retries, cancellation and private existing identity access. Forty-four Swift tests passed, including five actual URLSession tests using synthetic loopback HTTPS. macOS blocked native LAN access; CLI doctor passes. |
| Native granted app controls     | PR #26, `codex/native-actions`      | Implemented; GitHub runner startup failures                                        | Explicit target and app, one request at a time, bounded transport and truthful unknown outcomes after cancellation. Fifty-five Swift tests passed; real mini UI used synthetic clients. No new household action acceptance.                                    |
| Native current weather          | `codex/native-weather` at `54cfc17` | Implemented and pushed; branch CI passed; PR creation returns GitHub server errors | Twenty Swift tests passed. Real mini UI fetched a public-city forecast over HTTPS, refreshed, preserved the cache across restart, rendered the night symbol and cleared state on disable. Place selection is explicit and opt-in.                              |
| Native iPhone dashboard         | `codex/native-ios` at `528db10`     | Implemented and pushed; CI queued; PR creation returns GitHub server errors        | iOS simulator build and one expanded UI workflow passed: note edit/relaunch persistence, dashboard create/rename/delete and return navigation. Ten shared Swift tests passed. No physical iPhone, pairing or microphone acceptance.                            |
| Local transcription adapter     | PR #21, `codex/local-voice`         | Implemented; CI passed                                                             | Bounded local whisper.cpp WAV transcription, cancellation/deadline/cleanup; no audio logging or cloud fallback. Real model/microphone not validated.                                                                                                           |
| Physical-phone automation       | PR #20, `codex/delivery-queue`      | Runner implemented; device acceptance partial                                      | Safari WebDriver prerequisite/fake-driver checks; actual Mirroring connection and Settings navigation verified. User removed expired demo profile. No phone action remains pending; certificate installation/trust and physical WebDriver remain unverified.   |

Native dashboards and chores currently save on one device. These clients do not establish household synchronization, profile isolation or authenticated dashboard mutations. PR #24, PR #25, weather and iPhone are independent slices based on PR #23; PR #26 builds on PR #25. They have not been combined into one release. The voice adapter still needs native microphone capture and granted command routing. Weather is currently available only in its Mac slice.

## Next ready slices

1. **Native QR enrollment and revocable sessions.** Define the origin, certificate-pin and single-use invitation bootstrap plus a dedicated native credential lifecycle. Implement bounded pairing and Keychain storage with explicit household confirmation, revocation and rotation behavior; build on the iPhone foundation and existing scoped server grants. Do not require a system-wide certificate profile or reuse the browser cookie as a native credential. Use fixtures and simulator tests until real-device camera/signing acceptance is available.
2. **Native push-to-talk.** Explicit microphone consent, bounded transient recording, stop/cancel and transcript review, connected to the existing local transcription adapter and granted intent routing. No voice-derived authorization or silent cloud fallback. Installed model and real microphone acceptance remain separate steps.
3. **Playlist widget.** Explicit YouTube playlist IDs, title editing, user-started playback and unavailable/embedding-disabled states. Use an isolated player surface with constrained navigation and separate integration consent; no account-cookie reuse. A media embed does not make the application shell a web view.
4. **Read-only Calendar adapter.** Provider contract, bounded parsing, cached agenda state and synthetic fixtures first; OAuth and selected-account acceptance follow with explicit account consent.
5. **Native integration and acceptance.** Review the independent native branches together in an isolated integration checkout; preserve the open PR boundaries and live services. Combine chores, weather and granted controls, then verify shared model compatibility, reconnect and cancellation. Physical native LAN acceptance remains gated by the existing app permission step; integration review does not authorize deployment.

Stable private development identities still matter for repeatable browser compatibility tests. Preserve existing production identities, avoid regenerated certificate profiles each run, and keep ephemeral CI identities separate.

## Dependent slices

| Feature                       | Implementable sequence                                                                                                                                                                                                                               | External acceptance gate                                                                                                                          |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full local voice              | WAV transcription → native microphone capture → authenticated bounded audio upload where needed → native push-to-talk and stop/cancel → transient transcript review → granted intent routing → local spoken response → barge-in → optional wake word | Installed model, microphone consent, real speech/noise/latency checks. Voice identity may personalize; never grants access or tool permission.    |
| Shared dashboards and widgets | Native versioned local layout/editor → household store/migrations → authenticated per-profile API → synchronization/conflicts → reusable widget registry and configuration UI → import/export                                                        | Two clients, read-only TV, private/shared data isolation, revocation and offline/reconnect acceptance.                                            |
| Google Calendar               | Provider contract and synthetic fixtures → OAuth connection/Keychain → selected-calendar read-only sync → cached agenda widget → explicit event create/edit later                                                                                    | User-owned Google OAuth app/account consent. No live event writes until explicitly requested.                                                     |
| Gmail                         | Provider contract → least-scope OAuth/Keychain → bounded selected inbox metadata → private phone inbox widget → read selected content → drafts later                                                                                                 | User OAuth consent; mail privacy scopes reviewed. Sending/deleting messages is separately authorized, never inferred from integration setup.      |
| Chores and routines           | Local chores/chart → shared profile API → recurrence/reminders → deterministic routine scheduler                                                                                                                                                     | Time-zone/missed-run policy and no crash replay; notification destination consent where required.                                                 |
| Extensible widget ecosystem   | Built-in registry → typed configuration/data contracts → capability declarations → isolated external content → versioned third-party extension packaging                                                                                             | Untrusted widgets cannot execute arbitrary host code or inherit household credentials. Define sandbox and grant model before third-party loading. |

## Delivery rules

- Inspect current refs, instructions and open PRs. Verify write access on each working branch before substantial implementation. Preserve existing history and installed household services.
- Read this queue, active agent status and PR state before assigning work. One owner per file or worktree. Define interfaces before parallel dependent work.
- On completion: review the diff, run meaningful Node 24/Bun-managed checks, publish a small PR, record evidence and immediately select the next unblocked item. CI queueing is not a successful check. Never merge failed checks.
- Existing open PRs remain reviewable; merging the stack and deploying it require the applicable user authorization. A published PR is not a released feature.
- If a task blocks on a credential, consent or physical device, record the precise missing step once and continue another ready task. Do not repeatedly ask the same question.
- Maintain three concrete ready items. Replenish from the stated scope rather than creating unrelated work. Prefer usable vertical slices over placeholder-only PRs.
- All public fixtures are synthetic. Store OAuth credentials, phone identifiers, profile files, CA keys, model paths and household data outside the checkout. Exclude transcripts, recordings and private content from routine logs and failure artifacts.
- Background continuation is a scheduled local task, not a guarantee of uninterrupted execution. Checkpoint progress so host availability or usage limits do not lose work. Report only meaningful completions, blockers or decisions.

## Latest checkpoint

- PR #19 at `20a7032`: all GitHub checks passed after freezing the freshness-test clock. Phone demo acceptance is user-reported; no separate spoken-input evidence.
- PR #20: Node24 full check passed 162 tests with one platform skip; implementation CI passed. Mirroring subsequently connected and remote Settings/profile navigation worked. The owner completed expired-profile removal when iOS required the passcode. Cleanup is complete; do not repeat lock/unlock/removal instructions. No paired developer device was found; physical Safari WebDriver and certificate setup automation remain unverified.
- PR #21 at `c8c38e8`: complete checks passed164 tests with one platform skip. Cross-review hardened WAV structure/duration, bounded transcript reads, cleanup recovery and process cancellation. No local Whisper executable/model was available; microphone and real transcription remain unvalidated.
- PR #22 at `22558d6`: complete checks passed162 tests with one platform skip. Dashboard cases passed15 Chromium and5 WebKit checks; targeted legacy narrow-layout checks passed. Preview is browser-local, not household-synchronized.
- PR #23 at `243592c`: all GitHub checks passed. Native SwiftUI app built and code signature verified on the mini; real UI create/rename/note/restart checks passed. Export created a private file; import replacement confirmation was cancelled successfully. All nine synthetic Swift model/store tests passed on MacBook full Xcode. Node check passed 162 tests with one platform skip.
- PR #24 at `12e8f72`: all GitHub checks passed. Native chores, completion/undo and weekly chart built and signed on the mini. Real UI added a synthetic chore, completed it, observed the chart change, undid it and retained the pending task after restart. Fifteen Swift tests passed on MacBook Xcode; Node check passed 162 tests with one platform skip. Persistence is local, in a separate versioned file; export/sync, recurrence and notifications remain pending. No full VoiceOver acceptance is claimed.
- PR #25 at `f85c42f`: all GitHub checks passed. Forty-four Swift tests passed on MacBook Xcode, including credential boundaries, bounded helpers, trust validity, reconnect/selection state and five synthetic loopback HTTPS cases using the real URLSession transport. Node check passed 162 tests with one platform skip. Final release app built/signed on the mini; real Devices UI exercised. Native LAN access is not validated: the operating system reported `Local network prohibited`, while the existing CLI doctor successfully reached the same coordinator with a fresh four-capability node. Detailed evidence is in that branch's `docs/validation/2026-09-13-native-connection.md`.
- One owner step remains for native LAN testing: allow **Ellie** in macOS System Settings → Privacy & Security → Local Network, then reconnect the Node identity. The app now includes a usage description and actionable error text. No privacy setting was changed; do not bypass the permission through another process. Stable Apple-issued signing and notarization remain separate release work. Existing services, identities and Keychain items were preserved.
- PR #26 at `aa32ba7`: 55 Swift tests and Node 24 full checks passed (162 tests, one skip); final release bundle built and signature verified. Real mini SwiftUI exercised app selection, success, uncertain outcomes, stop-waiting, target changes and disabled offline/incapable controls using injected synthetic clients. No real command was dispatched. GitHub started different jobs successfully across push/PR runs, but remaining jobs failed runner acquisition before any test step. Retried failed jobs; the latest PR run remains queued. Do not call this PR's CI green.
- Weather branch `codex/native-weather` at `54cfc17`: exact-commit branch CI passed. Twenty Swift tests and Node checks (162 passed, one skip) passed; final release bundle built and signed. Real mini native UI and production HTTPS provider fetched public San Francisco city-center weather, refreshed, persisted after restart, displayed the night icon and cleared the private cache on disable. Outage, malformed-data and cancellation cases used fixtures. The app used isolated state; no household location, identity or Keychain entry changed. Repeated PR creation attempts returned GitHub server errors; read-back confirms no PR yet.
- iPhone branch `codex/native-ios` at `528db10`: generic simulator build passed, one expanded UI workflow passed on an owned iPhone 16 / iOS 18.3.1 simulator, and ten shared model/store tests passed on MacBook Xcode. Node checks passed 162 tests with one skip. The temporary simulator and derived data were removed. CI is queued; PR creation returned a GitHub server error and read-back confirms no PR yet. QR enrollment is requirements-only; physical iPhone installation, camera, microphone, Files-provider round trip and accessibility review remain untested.
- Publication follow-up: retry weather/iPhone PR creation after checking for an existing head PR, and inspect PR #26 runner recovery. Git push and read APIs work; do not duplicate PRs after an ambiguous create response. No open stack was merged or deployed.
- Next assignments: native QR/session enrollment, native microphone capture and selected-playlist playback. Keep Google OAuth/provider scaffolding synthetic until account consent is available; never request account secrets in chat.
