# Life harness overnight build

The user requested an overnight implementation with Sol engineers working in parallel. The owned checkout is `/Users/ryan/ellie-life-harness`, branch `codex/life-harness-build`, based on `c88c58e354e4da2dd831c5cc11447f406c41e1ed`. The original `/Users/ryan/ellie` checkout and its planning edits remain preserved. This work does not deploy to the existing household installation.

## Coordination

- `life_memory` (Sol) owns `packages/life-core`, `apps/life`, and their tests/docs.
- `task_runtime` (Sol) owns `packages/task-runtime`, `packages/life-harness`, `packages/life-import`, and their tests/docs.
- `life_interface` (Sol) owns `apps/life-ui`, `packages/life-ingest`, and their tests/docs.
- Root owns `packages/life-plugins`, `packages/life-context`, `packages/life-learning`, `packages/life-teaching`, root/CLI integration and acceptance. Root owns model transport, `packages/life-harness/src/model-context.ts` and `tests/life-model.test.ts`.

All engineers share the isolated worktree. Root coordinates commits after writers quiesce. Do not modify live installation state, Keychain, services or other worktrees. The separate task **Add macOS launchd services**, ID `01a09480-7b02-7d33-8c1e-ec9fc2894c15`, owns installer/native lifecycle/recovery. Avoid its `ServicePayload*.swift`, `PackagedServiceLauncher.swift`, `scripts/test-ios.mjs` and service-payload installer tests. No main merge or production deployment is part of this checkpoint.

## Working implementation

This is a working opt-in local application with real persistent data. It is not a production release of every feature in the broader product plan.

- **Life records:** private SQLite memory, people, places, commitments, needs, routines, sources and feedback; enforced user/group access; revisions; source provenance; atomic settings and imports. Settings resolve defaults, active group, user and task preferences separately from authority.
- **Teaching:** text, Markdown, HTML, email/transcript content, bounded local DOCX paragraph/heading/table extraction, PDF text extraction and PDF/PNG/JPEG OCR through macOS PDFKit/Vision. The sequential twenty-file queue supports mixed outcomes, progress, retry and cancellation; raw binary uploads accept up to 50 MB with two active uploads, deadlines and no late persistence after cancellation. Original files are not retained after extraction. Private temporary files, size/deadline limits, cancellation and process cleanup apply. Scoped retrieval includes references; source replacement/deletion removes chunks and invalidates dependent records. Explicitly adopted guidance has retained versions, source revisions, pause/resume and bounded model context; ordinary content remains inert. DOCX embedded media is not OCRed and external resources are never fetched.
- **Imports:** ICS/vCard preview, selection, server reparse, atomic commit, stable reimport identity and conflict handling that preserves user edits. Unsupported recurrence and ambiguous dates produce warnings.
- **Conversation:** supported commands remember/correct/forget facts, link birthdays/gift budgets/preparation, set timers/reminders/daily or weekly routines, finish/cancel needs, retrieve sources, evaluate explicit shopping context and create/revise/roll back apps. An optional literal-IP loopback model supplies broader replies and custom HTML generation with bounded requests and scoped personalization.
- **Background work:** durable tasks, time-zone/DST schedules, parent/child primitives, dependencies, scoped capabilities, cancellation, progress, missed-run handling and uncertain outcomes. Watch lifecycle and retention are bounded. Shared elapsed tree deadlines are explicit. Source summaries now use an atomic root with up to four parallel source workers, concurrency two and a shared deadline. Current source revisions are rechecked before returning/citing results. Explicit reruns create a fresh workflow while retaining the old result as history.
- **Extensions:** versioned API, guarded updates/rollback, retained revisions, bounded storage, an actual playable arcade shooter/high-score widget and live MLB standings/games. Provider requests coalesce/cache and label failures/staleness. Custom apps require a configured local model and receive only storage capability. Manage controls expose correction, revision history, rollback with retained data and intentional removal. Group storage identities are encoded to avoid cross-user key collisions.
- **Proactivity:** fresh explicit shopping/location/price/preparation signals matched to open needs, dates, budgets, currency, quiet hours and cooldowns. The local preparation monitor checks timed events within 48 hours and all-day events today or in the next two local calendar days on startup and every fifteen minutes, rotates authorized scopes, respects privacy pauses, and deduplicates active preparation notices. All-day expiry follows the actual day boundary in the effective time zone. Rescheduling beyond the preparation window hides an old notice. Notification/cooldown writes are atomic. Completed/cancelled or invalidated-source records stop suggestions; raw coordinates are not retained.
- **Learning:** rated/corrected examples, inspectable feedback, explicit personal export selection and bounded local JSONL export. No automatic upload, model-weight change or RL training.
- **Personal controls:** generation-bound paged exports across records, tasks, watches, progress, private apps, retained code versions and owned app storage. Browser archives have a total 10,000-item/25-MB limit. Reviewed reset pauses actor mutations, settles requests and private callbacks, invalidates conversation context, and journals deletion across stores. It restores the pause after a crash, exposes retry after reload, and prevents concurrent reset/drain callbacks from deleting newly admitted data. Shared group data and memberships remain; the actor's own encoded storage inside group apps is removed. Core, runtime and plugin schema-v2 migrations preserve prior data.
- **Service/client:** authenticated loopback HTTP, one-use fragment token, HttpOnly SameSite session, Host/Origin checks, limits, private state and graceful draining. Chat, Today, Your world, Your space, Activity and Settings use real APIs with empty default data. Visible views refresh every five seconds/on focus with scope/race guards; closing the arcade refreshes its score immediately. Bounded SQL summaries, separate paginated agenda/notifications, lazy full details and scoped search let sources grow without hiding commitments. Calendar helpers handle malformed dates, yearless/leap birthdays and extreme time zones. Activity exposes valid controls, readable progress and verified cited results.
- **Plugin isolation:** trusted outer broker and nested opaque plugin document. A trusted bootstrap creates the original document channel; navigation cannot acquire a fresh host port. Browser tests verify ordinary persistence and rejected navigation/storage attempts. Generated code never runs on the server.

## Latest verified implementation

Use Node 24 explicitly: `/Users/ryan/.volta/tools/image/node/24.21.0/bin`. Ambient version-manager resolution sometimes selects Node 22.

- Frozen dependency installation passed.
- Full `bun run check` passed after eleventh-slice integration: lint, formatting, generated contracts, all TypeScript projects, **562 tests: 561 passed, one existing skip, zero failures**, and both web builds. Test duration: 52.66 seconds. Latest log: `/tmp/ellie-life-check.log`.
- The final artifact-enabled `bun run life:test` passed against the actual authenticated service with private temporary databases. It covers memory create/edit, real PDF extraction, source retrieval, imports, birthday/gift completion, idle timer delivery, learning export, settings, arcade play/storage/reload, MLB data or honest failure, mobile layout and plugin isolation. Expanded acceptance covers full source editing, source search/pagination, an older appointment surviving 100+ newer records, malformed/extreme-zone dates, app revision/rollback/removal, stale result suppression and a distinct fresh summary rerun. Third-slice acceptance also covers source-linked guidance revisions/rollback, a complete personal archive excluding shared fixtures, restart into a pending reset, UI discovery/retry, and private deletion preserving shared content and memberships. Fourth-slice browser acceptance exercises an actual DOCX larger than the old base64 upload ceiling alongside a text file and a malformed DOCX, preserving separate successful and failed results. Fifth-slice acceptance covers private conversation restoration, duplicate request recovery without repeat effects, starting a new conversation during pending work, history deletion, private group-context chats, and reset removing transcripts. Final log: `/tmp/ellie-life-e2e-agent.log`; the seventh-slice owned listener on `127.0.0.1:52303` closed after the passing run. Sixth-slice acceptance covers durable reminder drafts, past/ambiguous time answers, exactly-once scheduling, same-record rescheduling with task replacement, retained earlier drafts and revisioned clearing. Seventh-slice acceptance adds the synchronous storage SDK, quota/error/timeout and navigation checks, legacy arcade/MLB compatibility, and an unchanged actual-model counter with persistence, Reset and failed-read/write recovery. Root reviewed the generated-counter screenshot and verified its fixture SHA-256 matches the original output.
- Artifacts: `/tmp/ellie-life-e2e-artifacts/`. Final screenshots were inspected after correcting compact MLB summaries, accurate date grouping with month/year, completed-item filtering, and a visible first arcade target. Browser coverage now proves the first actual shot scores 130 points. Retained app history exposes metadata without copying generated HTML into normal refreshes.
- No live household services changed. Tests close their own services and remove their temporary state.

## Current checkpoint and next work

The first implementation is `61e1da1` (`Build the local Ellie life harness`). The second is `fe67f53` (`Add coordinated learning and inspectable life workflows`). The third is `9038bed` (`Add personal data controls and automatic preparation`). The fourth checkpoint is titled `Handle calendar boundaries and document upload queues`; resolve its exact commit using `git log -1 --format=%H --grep="Handle calendar boundaries and document upload queues"`.

The third slice adds coordinated personal export/reset, recoverable UI controls, full guidance revision/review controls and automatic preparation checks. Root reviewed reset admission, restart and drain races, owner-bound cursors, archive memory limits, and final modal styling. Service tests cover concurrent reset starts, retry of an earlier completed reset, pending HTTP mutations, interrupted purge and a crash between the life and runtime journals. Runtime tests cover a stale drain preserving newly admitted work. No real user data has been deleted.

The fourth slice adds the shared `packages/life-time` module, precise local-time conversion and all-day preparation, today/tomorrow/next-weekday conversation, actual leap-day birthdays, and kind filtering before bounded chat queries. Tests cover half-hour DST changes, extreme zones, skipped midnights/dates, early Gregorian years and more than 500 unrelated sources. DOCX extraction uses bounded ZIP validation and a single-pass XML tokenizer; tests cover malformed archives, namespaces, entities and the formerly quadratic many-open-paragraph case. The file queue reports mixed successes without discarding earlier queued files or silently dropping overflow. Both binary routes prevent an aborted noncooperative extractor from persisting a late result.

The fifth slice implements conversational flexibility and continuity, after the fourth accepted commit `3db60b6`. A shared life-operation dispatcher executes strictly validated model intents for reminders, events, needs, contacts, agenda queries and source summaries. Trusted supplied history and context-generation checks protect foreground modeled work. Schema-v3 actor-private conversations provide idempotent request recovery, compact context fingerprints, list/detail/delete routes and personal reset/export integration. The interface restores durable history and action receipts, supports new/deleted conversations and recovers uncertain requests without blindly resending effects. Root's local model-readiness probe checks the configured exact model through a bounded inventory request without inference. Strict shared instant parsing rejects impossible dates rather than accepting native date rollover.

Fifth-slice review resolved deleted-request replay protection, epoch invalidation after reset, same-clock settings changes, compact fingerprint reads and reserved completion capacity before effects. Multi-record operations validate first and compensate scheduling failures. Owned input errors are distinguished from unexpected partial failures; an unknown result cannot be reported as “no changes.” Chat restore cleanup no longer aborts requests during ordinary bootstrap refresh; stable pending requests survive reload/scope transitions. Historical action receipts are retained. Full checks and final browser acceptance passed. Browser QA also fixed the history overlay, loading state, focus behavior and durable-restore assertions. The fifth checkpoint is titled `Keep conversations private and expand conversational actions`; resolve its exact commit with `git log -1 --format=%H --grep="Keep conversations private and expand conversational actions"`.

The fifth slice was accepted as `aad347b`. The sixth slice was accepted as `32a6741` (`Carry reminder requests through clarification and rescheduling`). It adds durable actor-private typed pending intents with 24-hour expiry, origin/answer/revision/context binding and reset/export integration; deterministic missing-time clarification; current-turn revision-bound corrections; and prepared reminder-task replacement. Drafts survive reload and expose revisioned clearing. Ordinary authorized reminder/event actions do not add a confirmation step.

Sixth-slice recovery follows the actual reminder record pointer across the core/runtime journal boundary. Pre-CAS preparation is discarded; post-CAS replacement is activated and the old task cancelled. Fault injection through authenticated chat verifies recovery when the record changed but the journal callback failed before its transition. Failed cleanup remains recoverable and blocks destructive personal reset. Past answers retain the draft and never display a completed receipt.

The exact model contract now includes separate nonexecutable drafts, formatted current local time, a 64-KiB serialized UTF-8 input ceiling, whole instructions/memories, a contiguous history suffix and disclosed source excerpts. One bounded JSON/schema repair shares the original deadline; transport failures never trigger a retry. Eleven focused model tests pass. [Real local model acceptance](life-model-validation.md) passes reminders, events, needs and missing-time drafts through the actual harness and private fixture stores. A generated counter still failed semantic persistence because it used browser storage; the seventh slice is adding a trusted storage SDK and accepting a freshly generated app in the real plugin sandbox.

The seventh slice was accepted as `d45dcb6` (`Give generated apps a persistent storage SDK`). The server supplies an immutable synchronous `window.ellie.storage` API and retains the original upstream port behind a translated legacy channel. Browser acceptance passes persistence, quotas, timeout, denied capability and navigation checks. Harness generation has a referenced 90-second default/120-second maximum deadline, retained admission slots, abort/context/scope/version checks and typed errors. Build, revise and chat model work abort during reset/shutdown; ordinary chat disconnect remains recoverable. Fourteen focused model tests pass. The actual local model’s SDK counter required two natural-language revisions; its unchanged output now passes browser acceptance for persistence, Reset and failed-read/write recovery. Its permissive value parsing and nonsemantic retry control are documented limitations. The exact fixture is excluded from automatic formatting to preserve original bytes. Full canonical checks passed after correcting that fixture formatting rule.

The owned temporary local model runner on `127.0.0.1:39473` was stopped cleanly after actual-app acceptance (verified PID 85980, exec session 69508). Its hash-verified runner/model and synthetic outputs remain in `/tmp/ellie-life-model-acceptance`. No saved Ellie model configuration changed. No inference or browser test process remains running.

The eighth slice was accepted as `891916f` (`Add shared spaces and conversation-scoped preferences`). It implements owner-controlled named local groups in the interface/chat and bounded actor-private conversation preferences. Schema-v5 migrations retain prior groups and add revisions; HTTP-created groups use opaque generated IDs, a 32-owned-group bound and owner-only revisioned rename. Group creation never moves existing personal records or switches context without the user opening it. Duplicate visible names require disambiguation. Temporary style persists with the private conversation; explicit lasting instructions change personal preferences unless a shared setting is explicitly requested with authority. Preference updates bind to the pending turn, data epoch, context fingerprint and preference revision, and complete atomically with the durable turn.

Eighth review caught private group-context response ratings being scoped to the group. Ratings now bind to an owned accessible completed turn, use the durable prompt/response rather than client replacements and write personal feedback only. Generic feedback and personal preference corrections also remain personal by default. Browser acceptance proves group create/open/rename, stale CAS refresh, private records and private group conversations, persistent temporary preferences, clear-to-inherited behavior and private response ratings. Root reviewed `/tmp/ellie-life-e2e-artifacts/life-shared-spaces.png` and `life-conversation-style.png`; final current-tree acceptance passed at `127.0.0.1:57919`, with the listener verified closed afterward.

The model receives a bounded partial selection of people, needs, places and commitments, including a small number of same-space relationships. Whole projected records have an 8-KiB allowance inside the shared 64-KiB ceiling. Oversized memory facts are omitted whole rather than losing a trailing qualification. Four world-context tests and sixteen adapter tests pass. The real six-case local harness acceptance passes with inference counts separating host parsing from model calls; it adds personalized gift advice and empty-group privacy. Earlier failures from flat clarification actions and overstated gift affordability are documented in the model validation report. Ordinary polite reminders with missing times now use the deterministic draft route; supplied temporal phrases must remain available to existing parsing or the model.

The second owned runner used for that acceptance (PID 11192, exec session 7580, `127.0.0.1:39473`) was stopped cleanly after testing. Its synthetic output is `/tmp/ellie-life-model-acceptance/world-final-cases.log`. No saved model configuration changed. Do not claim invitations, synchronization, multi-device identity or model-weight training.

The ninth slice was accepted as `777afed` (`Turn private corrections into reviewable guidance`). It implements the [private improvement loop](life-improvement.md): one to three selected private examples, a bounded candidate instruction, offline replay, user review and explicit adoption into normal private versioned guidance. Two actual workflows and a ninety-second default whole-workflow deadline bound work; pre-abort, synchronous abort, stale inputs and late callbacks cannot save output. Reset/shutdown wait for underlying callbacks to settle. Retention uses a private SQL marker filter before the twenty-proposal cap. Invalid model output, transport failure, full retention and active concurrency have distinct actionable errors.

Adoption transforms the same record atomically. Original candidate instructions and audit metadata survive later guide revisions and the eight-version retention window. Editing an unadopted instruction invalidates its old previews. Source-feedback changes suppress normalized and raw-DTO replay payloads; adopted user-retained rules remain independently controllable. Ready/dismissed proposals and teaching records are excluded from read-only world context, so only active guidance resolution can introduce instructions.

Final browser acceptance passed at `127.0.0.1:58828`, then verified that listener closed. It covers selection separate from export, no preview mutations, no active guidance before adoption, explicit adoption, reload, dismissal, stale-input rejection and private proposals in group context. Root reviewed `/tmp/ellie-life-e2e-artifacts/life-improvement-review.png`. Twenty-two adapter and five world-context tests pass. The real local-model engine acceptance passed in 14.1 seconds and is reproducible with `scripts/verify-life-improvement.mjs`. The model reused example wording and added an unnecessary condition; these limits are recorded without claiming a quality benchmark. The third owned runner (PID 18989, exec session 44269, `127.0.0.1:39473`) was stopped cleanly. Synthetic results remain in `/tmp/ellie-life-model-acceptance/improvement-final-engine.log`.

The tenth slice was accepted as `d752c13` (`Keep scheduled delivery controls and status consistent`). It adds schedule lifecycle controls and authoritative delivery status. Resume preserves the recurrence template and next scheduled occurrence; manual runs materialize children; cancelling a terminal one-shot template reaches active descendants without changing completed outcomes. Prepared replacements remain protected. Exact chat controls and service/UI details bind the life record, scope, owner, task handler and record reference. Kind filters run before bounded title scans, and partial windows cannot establish uniqueness. Direct SQL occurrence queries filter active state before limiting and use insertion order for same-clock ties, including histories beyond 500 runs.

Future schedule state and current/latest occurrence state are separate. A successful schedule template does not establish notification delivery; verified occurrence results distinguish delivered, skipped and unknown outcomes. Pausing delivery does not freeze a timer or complete an errand, and cancellation does not undo a delivered effect. Model world context includes these bounded status observations. Actual local-model testing caught a separate agenda read path still listing paused delivery as active; modeled and deterministic agenda reads now use the shared resolver and show other delivery states separately. The final nine-case synthetic run passed its scope/effect/status assertions; it does not establish general recommendation quality. Its gift answer again made an unsupported affordability claim, recorded in the model validation report.

Browser acceptance passed at `127.0.0.1:61119` and the listener closed. Root reviewed `/tmp/ellie-life-e2e-artifacts/life-delivery-status.png`. It covers routine create/list/pause/resume/cancel and retained history, with additional rendering assertions for an active occurrence under a completed one-shot template and skipped/cancelled delivery.

A machine-local preview launcher is prepared at `/tmp/ellie-life-model-acceptance/launch-preview.mjs`. It starts only the hash-verified local model assets and this checkout, with ephemeral loopback ports, a unique exact model alias and isolated `/Users/ryan/.ellie-life-preview` state. The authenticated restart smoke passed with zero records, tasks or apps, two available built assets and model readiness. `/tmp/ellie-life-model-acceptance/preview-smoke.json` records the result. Both smoke-launch processes were stopped through their own launcher handles, with ports verified closed: first PID 25504/runner 25512 (session 12748, ports 60559/60547), then PID 27588/runner 27590 (session 99108, ports 61216/61208). No model or preview process remains running. The empty preview state is retained for the morning. A fresh morning start must provide a new one-use launch URL; do not reuse a consumed or expired token.

The eleventh slice was accepted as `30af408` (`Carry saved plans and checklist progress through chat`). It adds [saved plans and checklists](life-plans.md): one scoped goal record, up to 24 stable steps, derived progress, revisioned completion/reopening, exact title lookup and a 64-plan bound. Model proposals save text only; no steps run automatically. SQL filters the plan marker before limits, malformed entries remain counted toward capacity and are isolated in list responses. Existing personal export/reset includes plan records and preserves shared plans. Chat and HTTP share the same implementation.

The Plans tab supports progress, completion/reopening, reload and stale-revision refresh. Root added request-generation guards across scope changes/unmount, prevented a late save from reopening a closed modal, and exposed refresh and malformed-record counts. Browser acceptance passed at `127.0.0.1:49531` and the listener was verified closed. Root reviewed `/tmp/ellie-life-e2e-artifacts/life-plans.png`. The test covers group isolation, stale updates, a deliberately delayed save followed by modal closure, persistence and chat reopening. The initial stale-update test incorrectly required a checkbox to become checked even when the server rejected the old revision; it now clicks and asserts the authoritative refreshed state.

Twenty-three adapter and seven world-context checks pass. The actual local model first returned only prose for a creation request; after clarifying the action contract, the plan creation/control/readback/restart acceptance passed in 12.7 seconds with two inference requests. The scope/effect assertions and exact synthetic output are documented in the model validation report.

Two Sol engineers hit the account usage limit during this slice; their edits were retained. Root finished UI integration and acceptance, and the remaining Sol engineer independently audited the core/service code with 45/45 focused tests passing. No reset credit was consumed. The preview launcher left running during the interruption (PID 36640, runner 36642, session 91608; ports 63050/63043) was stopped cleanly and both ports were verified closed after acceptance. A fresh preview start remains necessary for the morning handoff.

The morning preview and handoff are complete. Do not resume another feature slice without a new request. The separately owned native installer and live household services remain unchanged. The remaining integration limits below still apply.

The raw core-only deletion API remains a store-local primitive; the Settings workflow coordinates the full personal reset. Do not claim cross-database atomicity, external undo, erased downloaded exports or model unlearning.

Native continuous location, alerts during sleep, third-party account connectors, arbitrary server plugins, cloud model setup, multi-device identity integration and model-weight training remain separate work. Downloaded exports cannot be recalled by deleting their source. The app currently has one local trusted actor per process.

## Overnight continuation

Heartbeat `build-ellie-life-harness-overnight` was paused through the Codex automation tool at approximately **08:03 America/Los_Angeles on September 14, 2026**, after final integration and verification. The overnight build will not continue automatically. Owned `/usr/bin/caffeinate -i -t 31620` (exec session `54237`, started around 06:13 UTC) was time-bounded to approximately 08:00 local. It does not change saved power settings.

At or after **September 14, 2026 at 08:00 America/Los_Angeles**, finish the current bounded integration, run final checks, provide the morning handoff and pause this heartbeat. Keep unchanged runs quiet. Do not mark the entire vision complete because a development slice passes.

See the [runbook](life-runbook.md) for launch commands and [product plan](life-harness-plan.md) for the larger direction.

## Morning preview

A fresh isolated preview was started at 07:49:59 America/Los_Angeles and verified in the Codex in-app browser at `http://127.0.0.1:49569/`. The one-use token was exchanged successfully; the app shows an empty private conversation and the configured local model ready. No synthetic records were added to preview state. The user-facing browser tab is retained as a deliverable. Launcher PID 44333, runner PID 44335, exec session 49485; model endpoint `http://127.0.0.1:49562/v1`, exact alias `ellie-qwen4b-0c5b9960`. These owned preview processes are intentionally left running for the handoff. Do not stop them by port alone; verify their current command/ownership first.

A final default-greeting copy correction removes the placeholder name “You”; the UI build and visible browser reload passed. The [morning handoff](life-morning-handoff.md) provides working features, launch commands, verification and remaining dependencies.

The reviewed preview handoff is committed as `64db344` (`Prepare the verified morning preview handoff`). This final documentation checkpoint records the successful heartbeat pause. The isolated preview remains running for the user; there is no deployment or merge to main.

## September 14 dashboard and automatic-memory follow-up

The user rejected the replacement management interface and clarified that Ellie should keep its original configurable widget/board layout. A further correction moved conversation out of the widget grid entirely: a glowing, floating orb at the bottom center opens a focused conversation overlay. The existing mist-blue palette, Avenir typography, boards, widget ordering/sizing and full-screen generated apps remain the foundation; detailed tools stay secondary.

Automatic memory is now an internal prompt-to-Markdown pipeline. Schema v6 captures each authoritative private user turn, backfills retained conversations, prioritizes whole extractive facts/preferences/corrections, caches a bounded summary and injects it into later modeled conversational turns. Private atomic Markdown snapshots are derived from canonical SQLite records. Scope membership, request recovery, forgetting, deletion and personal reset share the existing authority and data-generation checks. Users do not upload Markdown for ordinary remembering. See [automatic memory](life-automatic-memory.md) for exact behavior and limitations.

The Sol interface and runtime engineers were continued with separate ownership. Root integrated the HTTP/model boundary, added actual service and model-input tests, and adapted browser acceptance to the restored dashboard and orb. An independent integration audit found no blocking issues and passed 27 focused tests. The real local model recalled an ordinary dietary preference in a new conversation after a complete service/store restart: two inference requests in 14.369 seconds. Its unsolicited initial dinner suggestions are recorded as a response-quality limitation. The summary is currently extractive selection, not semantic consolidation or weight training.

The final canonical `bun run check` passed: **568 tests, 567 passed, one existing skip, zero failures**, test duration **55,128.242 ms**, plus lint, formatting, contracts, all TypeScript projects and both builds. Log: `/tmp/ellie-dashboard-correction-check-final.log`. Browser acceptance passed at `http://127.0.0.1:56278`; it covers board persistence, orb centering/open/close, focus return, draft continuity/isolation, repeated reload, nested history, memory-backed actions, checklist refresh, plugins, sandbox failures, settings and reset. The test server closed afterward. A final mobile layout adjustment stacks narrow baseball summaries and uses Memory/Apps navigation labels; relevant checks are recorded below.

The owned old preview launcher and runner (44333/44335) were verified by exact command, stopped gracefully, and ports 49569/49562 were checked closed. Before migration, the closed private state was backed up to `/tmp/ellie-dashboard-migration-hcd64ile/state` with a private preservation report. It contained one conversation and four retained turns. No conversation text or credentials were copied into this repository.

The refreshed isolated preview started at **08:55:04 America/Los_Angeles** at `http://127.0.0.1:55869/`, using the same `/Users/ryan/.ellie-life-preview` state. Launcher PID **73488**, runner PID **73491**, exec session **63757**; local model endpoint `http://127.0.0.1:55860/v1`, exact alias `ellie-qwen4b-41c6849c`. The in-app browser successfully exchanged the new single-use link and visibly verified the dashboard orb, focused conversation overlay, existing four-turn history, existing generated game and four automatic conversation notes. One private mode-0600 Markdown snapshot was generated. No synthetic prompts or records were added to that preview. The updated dashboard is retained as a browser deliverable; only these owned preview processes are intentionally left running.

The overnight heartbeat remains paused; this follow-up is user-directed work. Native clients and live household services were not changed, restarted or deployed, and main was not merged.

Final follow-up verification: lint, formatting, UI TypeScript and the complete authenticated `bun run life:test` passed again after the mobile baseball/navigation changes, at `http://127.0.0.1:56848`; log `/tmp/ellie-dashboard-correction-e2e-final.log`. Both final synthetic listeners (56278/56848) were checked closed. Screenshots in `/tmp/ellie-dashboard-correction-artifacts` were reviewed for the board, orb overlay, history and mobile. Visual review then corrected mobile sheet alignment and close-button spacing with a CSS-only adjustment. The final production UI build passed, and the current authenticated preview was reloaded and checked through computer use at 390×844: bottom-aligned conversation sheet, visible composer, separate close/history controls, focus entry/return and no horizontal clipping. The temporary viewport override was reset and the desktop dashboard left open.

Review caught and fixed: stale URL ownership across repeated reloads, draft carryover through History, dependent checklist/memory refresh behind the overlay, and the orb intercepting controls in ordinary app dialogs. Existing browser expectations were adapted to explicit orb navigation and retained Memory tabs; assertions for stored actions, scope boundaries, sandbox behavior and reset were retained. No microphone permission or voice-capture implementation is implied by the orb's visual design.

## September 14 parallel speed and polish integration

User-directed Sol engineers implemented conversation progress, shared inference admission, selective automatic memory, quieter proactive suggestions and generated-app guidance; an Astra extra-high architect reviewed the combined changes. Boards and the bottom-center conversation orb remain the interface. Options now contains secondary chat controls, and widget/orb states distinguish pending work from empty or unavailable data.

Model output streams as provisional reply snapshots; the complete validated plan and host receipts remain authoritative. Drafts are transient, scope-checked and removed on repair, context change, completion or reset. One local inference runs at a time with three queued jobs; foreground chat and its repair precede queued background generation. A running generation still delays later chat. Automatic memory uses schema-v7 exact-scope revision counters and a 6-KiB query-specific selection with conservative deduplication and whole qualifications. The full prompt journal and separate cached Markdown view remain available.

Explicit proactive dismissals create private source/category evidence and back off from 24 hours to 30 days. Completion remains distinct and unfinished needs remain intact. The notification update and optional preference retention are separate writes: retention failure does not undo a successful dismissal. Generated-app prompts include responsive, accessible and recoverable SDK behavior; a conservative blank-document gate preserves dynamic scripts, including scripts in the page head. Static checks do not prove every generated application works.

Review caught and fixed head-script rejection, dismissal history hidden by more than 500 unrelated feedback records, desktop orb-label clipping and a lost brief-acknowledgment instruction. Browser acceptance exercises streamed drafts, repair clearing, one final reply, late callbacks after New, collapsed Options, persistent boards, reminder clarification, generated-app storage/retry/sandbox boundaries and desktop/mobile orb bounds. A dedicated Life browser acceptance PR workflow now runs the authenticated fixture automatically.

Frozen installation passed with no dependency changes. Before the final reviewed native-base integration, canonical `bun run check` passed 616 tests (615 passed, one existing skip), contracts, lint, formatting, all TypeScript projects and both builds; log `/tmp/ellie-polish-check.log`. Final combined checks and merge outcome are recorded below when available. Full browser acceptance passed at this checkpoint; log `/tmp/ellie-polish-e2e.log`.

Two actual local Qwen requests used only synthetic temporary stores. A correction from vegetarian to vegan, a Friday fish exception and a mushroom/truffle qualification survived a full service/store restart into a new conversation. Safe preview/final HTTP timings were 2.442/4.828 seconds and 1.814/8.303 seconds. First previews were only a word, and these warm-run measurements are illustrative, not a general benchmark. The temporary state was removed; existing user state and model endpoint were untouched during acceptance.

The native owner is integrating its reviewed release foundation separately. Its published `862fe876ce993da88f2befff3f6e5ab625a1001f` was merged into this owned worktree, preserving both current native roadmap content and the opt-in Life status. Life changes will merge only after the final base is coordinated and its own PR checks pass. No deployment, household service restart, Keychain change or device-permission request is part of this work. The overnight heartbeat remains paused.

Final combined canonical checks passed after integrating the reviewed native base: **632 tests, 631 passed, one existing skip, zero failures**, duration **56,551.694834 ms**, plus lint/format/contracts/types and both builds. Log: `/tmp/ellie-polish-check-final.log`. Browser reruns and CI exposed fixture timezone and teardown issues; those are being resolved before merge rather than treating UI assertions alone as a complete pass.

The owned old preview processes 73488/73491 were verified and stopped gracefully. Closed private state was backed up to `/tmp/ellie-polish-migration-wde6qrpw/state`; schema-v7 migration preserved every pre-existing table row across all three databases, verified by private hashes. The refreshed preview is `http://127.0.0.1:49632/`, started 10:12:37 America/Los_Angeles, launcher19840/runner19844, exec session15103; model endpoint `http://127.0.0.1:49626/v1`, alias `ellie-qwen4b-70358da2`. Computer use verified the retained dashboard/game, collapsed conversation Options and local-model readiness, then left the dashboard open as a deliverable. No synthetic records were added to user preview state.

The browser blocker was a stale test assertion (`Ellie is drafting…` versus actual `Drafting…`), which timed out before releasing synthetic model gates and masked the assertion with a shutdown drain error. All fixture gates are now created up front and released in cleanup; production shutdown semantics were preserved. The browser and Node fixture timezones are explicit. Both full reruns passed with exit0: UTC host at `http://127.0.0.1:50335` (`/tmp/ellie-polish-lifecycle-utc.log`) and normal host at `http://127.0.0.1:50387` (`/tmp/ellie-polish-lifecycle-normal.log`), including strict teardown. These supersede the incomplete orb-only reruns.

Hosted Linux browser acceptance then exposed a missing `.visually-hidden` utility: the orb's screen-reader live region was participating visibly in shell layout. The standard clipped utility now retains assistive-technology status while removing that stray layout item. The strict mobile overflow assertion remains; screenshots and bounded element diagnostics are captured before it. The complete local browser run passed again at `http://127.0.0.1:50843`; visual review confirmed the stray status text is gone. Hosted verification remains the merge gate.

Follow-up Linux bounds diagnostics identified the remaining overflow precisely: the nonwrapping dashboard title/actions row extended to x419.78 in a390px viewport. Mobile title/actions now stack and wrap, preserving all controls and the existing cards. The hidden-live-region fix was useful accessibility polish but did not alone resolve this separate header width issue. Full UTC browser acceptance passed locally again at `http://127.0.0.1:51340`; the mobile screenshot was reviewed before the next hosted check.

Hosted Life browser acceptance passed on Linux for head `599a00d`: run `34875309825`, duration1m18s. TypeScript and command-center jobs also passed on that head. This confirms the timezone, fixture cleanup and mobile header fixes under the CI platform; no overflow assertion was weakened. The final native/source union is coordinated separately before the PR merge.

The native owner supplied its complete reviewed readiness union `7a26f257f45abaf03f7ec8756ded127bf00e4b20` (certificate/speech diagnostics and signing-validation tooling). It was merged without conflicts into the Life worktree to run the final combined checks concurrently. PR88 targets main; its actual merge stays held until the separately owned prerequisite PRs land. No separate signing experiment or live-service operation was performed here.

The complete reviewed source union passed canonical local checks: **645 tests, 644 passed, one existing skip, zero failures**, duration **49,838.352542 ms**, with lint, formatting, contracts, all TypeScript projects and both builds. Log: `/tmp/ellie-polish-union-check.log`. PR88 remains held while the native owner diagnoses a hosted ATS fixture failure in its prerequisite chain; no failing check is bypassed or treated as proof of a particular cause.

All hosted checks passed on the preceding complete Life union `a1fd8ca`: native run34875680299/job104082147671 (14m30s), TypeScript, command-center, Life browser acceptance and GitGuardian. That evidence is retained separately from new-source acceptance. The native owner then supplied reviewed correction `85a6cfacb2bfb73cb3637c8bf73aa38e0730157a`, which only updates two native tests plus validation documentation: use the existing production timeout in the ATS fixture and test a delayed HTTPS response. It was merged without conflicts. No production transport or Life behavior changed. The resulting exact head receives fresh full hosted checks before merge. Final hosted results and merge status are attached to [PR88](https://github.com/rdimascio/ellie/pull/88).

## September 14 connected-life implementation

PR88 completed at actual merge `a4ee32864712fd9524476d1f1a7280bd06f01df6`, with tested tree `4ea2ea1e9c63ab3efb76a59c8e5e476b56545cd4`. The subsequent user request makes account connection the starting point for automatic assistance. The user explicitly rejected a command checklist as the ordinary product flow. This new slice preserves the dashboard, plan widget and floating orb, adds secondary connection controls in Settings, and creates private preparations from evidence without impersonating a chat request.

Sol engineers implemented read adapters, pure anticipation, credential handling, preparation delivery and UI acceptance in parallel; an Astra extra-high architect owned orchestration, the trusted connector registry and independent review. The source integrates actual reviewed main `36fb0bb78f500adc04b5718ef3828407bfa3c76b` (PR91). Native activation/installer ownership remains in the separate task; this slice makes no changes to those runtime paths.

The connected loop is real: identity-verified Google Calendar/Gmail/Plaid adapters → private incremental evidence → durable calendar/communication/spending specialist tasks → combined current-evidence publication → inferred memories, private plans, one-time reminders and supported weekly routines. The initial specialists use deterministic rules; their persona prompts do not imply model-based research ran. Hourly sync and daily maintenance cover four planning horizons while the Life process runs. The registered connector plugin family is a trusted server API; generated widget HTML never receives tokens or arbitrary network/host execution.

Google's loopback authorization uses a registered desktop client, PKCE, one-use state, read-only scopes and refresh. On macOS, the host opens the OS browser because embedded Google sign-in is unsupported; fallback provides an explicit external-browser link. Downloaded client configuration must be an owner-only private file outside the repository. The vault is AES-GCM encrypted with a separate mode-0600 installation key; this relies on OS account/file protection, not Keychain or hardware isolation. No actual account was connected or read during development.

Review fixed expired-cursor evidence retention, initial-page window drift, Plaid pagination restart, partial-batch publication, older correction coverage, omitted-inference withdrawal, credential refresh resurrection, default-browser sign-in, reminder mapping replacement, cancelled-reminder resurrection, local quiet-hours handling and monthly events incorrectly classified as weekly. Three consecutive weekly local dates are now required for an automatic weekly routine; future matching bookings suppress duplicate routine creation. User edits and deletions are respected, and source/mode/revocation changes stop obsolete deliveries. Export includes connected metadata/evidence/receipts without secrets, and the existing durable personal reset drains tasks and deletes pending OAuth state as well as account data.

The first canonical integration passed **684 Node tests: 683 passed, one existing skip**, plus lint, formatting, contracts, all TypeScript projects and both builds; `/tmp/ellie-connected-check.log`. This is an intermediate checkpoint, superseded by the final source checks below. The strengthened connected browser fixture exercises actual queued sync/research/publication, sessionless OAuth callback and replay rejection, unchanged mobile board/orb, plan reasons, standing modes, archive contents, disconnect and reset. Initial reviewed screenshots are in `/tmp/ellie-connected-browser-review`; the first successful connected UI run used `http://127.0.0.1:57736` and closed its fixture afterward.

Remaining dependencies are explicit: registered Google client/provider approval; Plaid Link onboarding; other daily-life service adapters and typed external actions; cross-account/model-based research and external weather/news/event analysis; stronger-device routing; complete financial coverage and cash-flow estimation; and authoritative provider receipts for automatic checklist completion. A weekly habit can now create a guarded routine, but a message snippet cannot mark a form submitted. No purchases, cart changes, messages, calls, bookings, financial transfers, vehicle actions or home controls were executed. No live household services, `~/.ellie`, Keychain, device permissions or other worktrees were modified. The overnight heartbeat remains paused; this is user-directed follow-up work.

Final canonical local checks passed on the PR91 source union: **695 tests, 694 passed, one existing skip, zero failures**, duration **55,690.278416 ms**, plus lint, formatting, generated contracts, all TypeScript projects and both builds. Log: `/tmp/ellie-connected-check-release.log`. Frozen installation passed with only the two new workspace package entries in `bun.lock`; no external dependency was added. Full original UI acceptance passed at `http://127.0.0.1:58641` with a UTC host (`/tmp/ellie-connected-full-browser-final.log`), and connected UI acceptance passed at `http://127.0.0.1:59225` (`/tmp/ellie-connected-browser-final.log`). Both fixtures exited cleanly. Final mobile screenshots in `/tmp/ellie-connected-browser-final` were reviewed for the preserved layout, account controls and localized appointment reason. Astra's final independent audit reported no remaining blockers in this bounded flow. Hosted checks and the actual merge result are recorded on the pull request.

## September 14 shared native Life development integration

This slice starts from reviewed main `0740fe0665b0d6bce08525b40bd017e86c5623a0` (PR94), after the connected Life work merged through PR93. It preserves the existing dashboard, floating orb and native controls. It is source integration for a shared development release; the running authenticated local preview has not been replaced with this source.

Scheduling now has one owner. `finish-ellie-service-validation` remains the active thirty-minute heartbeat in **Add macOS launchd services** (`01a09480-7b02-7d33-8c1e-ec9fc2894c15`). `build-ellie-life-harness-overnight` stays paused. That task owns packaging, signing, lifecycle/OTA, the full combined gate, serialized main merges and the consolidated owner QA checklist. This task owns Life runtime/routes/grants and native Life clients, and supplies exact source and focused test receipts. Existing Sol engineers and the Astra extra-high architect are reused with separate files; unchanged complete-source checks are not repeated across lanes.

The coordinator can host the real Life application in-process on its existing native HTTPS listener. Explicit persisted `life.account` grants map enrolled devices to one configured canonical Life actor, including that actor's existing Life groups. Pairing or a household label does not create this grant. Native bootstrap issues a separate memory-only thirty-minute web session. A nonpersistent WebKit container installs a Secure, HttpOnly, Strict cookie and loads `/life/`; the native bearer never enters JavaScript or a URL. macOS adds an independently enrolled Life window; iPhone adds Open Ellie Life to existing enrollment. Thirty-second pinned native authority checks clear a revoked or unavailable view, while the server rejects unauthorized requests immediately.

The embedded handler reuses Life ownership, request validation, memory, plans, tasks, generated apps, imports and account controls. Original request authority follows asynchronous model/extraction work so late results cannot commit after access ends. Already journaled personal reset recovery continues as host work. Gateway shutdown drains actual embedded handlers before stores close. Remote account setup clearly directs the user to the host Mac; cross-device OAuth handoff is not implemented.

Source setup is deliberately explicit for this development milestone. On an already initialized, isolated coordinator with native HTTPS configured, build both web clients, provide a private `0600` JSON file outside the checkout and `~/.ellie`, and select it with `bun run ellie server start --life-config /absolute/private/life.json` (or the path-only `ELLIE_LIFE_CONFIG`, not both). The minimum schema is `{"version":1,"stateDir":"/absolute/private/life-state","actorId":"owner"}`; the separate state directory must be private `0700`. Optional `modelUrl` and `model` configure the existing literal-loopback model, and `googleOAuthClientFile` references private host registration. Then pair each native client using the existing native invitation flow and use `bun run ellie life-access grant CLIENT_ID owner`; `life-access grants` lists access and `life-access revoke CLIENT_ID` removes it. These are source-development instructions, not permission to initialize or restart the user's current household services.

Packaging consumes `@ellie/life/embedded`, its workspace dependency closure, and `apps/life-ui/dist`. The release owner has the exact API/configuration contract and owns adding those assets to the payload and forwarding trusted configuration. `bun run life:test:native` is the dedicated combined TLS/browser acceptance command. It uses isolated synthetic accounts, certificates, stores and Chromium contexts, never the retained preview.

Focused acceptance currently includes shared records, conversations and automatic memory across embedded restart, private/group isolation, original-authority cancellation, durable reset recovery, persisted device grants, failed persistence refusing access, two actual TLS-paired browser clients sharing plan changes, selective revocation, and session tokens hidden from page JavaScript. Browser QA also creates and plays Star Arcade through the gateway, persists a high score, checks account setup guidance and verifies the unchanged desktop/mobile layout. Screenshots are emitted under `ELLIE_E2E_ARTIFACT_DIR`; the final reviewed local set is `/tmp/ellie-shared-life-browser-final`. Final source checks and native evidence follow below when complete.

Remaining release dependencies are explicit: full Xcode/WebKit and iOS checks on the coordinated native test machine; a packaged development artifact containing this exact source; physical-device installation/signing and owner QA; host account registration and actual provider consent; and the separately owned deployment/OTA activation work. Chromium proves the shared HTTP/UI flow, not WebKit cookie-store behavior or a physical iPhone install. The broader integration catalogue, distributed inference, purchases, booking, messaging and model-weight training remain outside the implemented slice. No live service, Keychain, permission, `~/.ellie` or existing preview data was changed.

Final focused validation on this Life integration (Node 24.21.0, Bun 1.4.2):

- `bun install --frozen-lockfile --ignore-scripts` passed; only three workspace dependency entries changed in the lockfile, with no new external package.
- `bun run typecheck`, `bun run lint`, `bun run format:check`, `bun run contracts:check` and `git diff --check` passed. Logs: `/tmp/ellie-shared-life-{types,lint,format,contracts}-final.log`.
- The following focused Node command passed **115/115**, zero skips/failures, in **21,262.988375 ms**. Log: `/tmp/ellie-shared-life-focused-final.log`.

```sh
node --test tests/life-service.test.ts tests/life-auto-memory-service.test.ts tests/life-chat-progress.test.ts tests/life-embedded.test.ts tests/native-life.test.ts tests/native-life-persistence.test.ts tests/life-config.test.ts tests/life-access.test.ts tests/life-lifecycle.test.ts tests/browser-server.test.ts tests/browser-runtime.test.ts tests/browser-management.test.ts
```

- `node --test tests/native-life-gateway.test.ts` passed **1/1**, in **20,478.4185 ms**. It holds an authorized response for 10.2 seconds, verifies usable sessions after capacity pressure, and confirms a drip-fed legacy native upload closes at its ten-second absolute body deadline. That upload timer ends when the request body completes, preserving longer native actions. Log: `/tmp/ellie-shared-life-gateway.log`.
- `ELLIE_E2E_ARTIFACT_DIR=/tmp/ellie-shared-life-browser-final bun run life:test:native` passed with exit zero, including a fresh production UI build and fixture teardown. Log: `/tmp/ellie-shared-life-browser-final.log`. Final desktop and mobile screenshots were visually reviewed; the earlier arcade screenshot was also reviewed. The UI retains the dashboard and orb.
- `swift build --package-path apps/desktop` passed during native source review. The eight `LifeWebSessionTests` cases require the release owner's full Xcode toolchain; local XCTest compilation is unavailable. No passing native-test result is inferred from the application build.

Astra independently reviewed the cold application ownership and native asynchronous state fixes. The coordinator retains the application before job preparation, stops admission before draining, suppresses readiness after a concurrent stop, and retains its lifetime lock when draining fails. Native renewal and foreground checks reject stale callbacks and cannot install a discarded session into a new view. The source is ready for the release owner's combined gate and packaging integration; physical-device/manual acceptance and merge are still pending. This task has not performed a second broad canonical gate or duplicated the release owner's main merge.

## September 14 agent-browser verification loop

The quality lab now drives the built Life dashboard and orb with pinned agent-browser 0.37.1, a private named browser session, isolated stores and the actual HTTP/task handlers. `bun run life:quality:browser` rebuilds the UI before the run. Reports keep source, fixture, runner and built-asset digests, scenario outcomes, snapshots and screenshots. Failed development runs remain in the private ledger; they are not reclassified as product failures or passing evidence. The existing two-hour quality heartbeat now prioritizes this browser loop before owner-facing manual checks; the release task retains its separate thirty-minute cadence and merge ownership.

An exploratory pass used a separately reserved local Qwen model for three completed UI turns against new synthetic user state. An ordinary peppermint-tea statement was recalled in a new conversation and no longer recalled after Forget in the same conversation. This was real model behavior through the orb, with no live preview prompts. The initial exploratory fixture did not capture outbound requests, so this evidence cannot establish exact prompt contents or inference latency. The response after Forget inaccurately said the detail had not previously been shared; that wording and an unnecessary preference follow-up remain model-quality observations.

That pass reproduced a false recovery warning while a normal request was still drafting. The recovery request is deliberately persisted for interruption handling; the chat now hides its recovery banner while the active request is busy, preserving it when transport ends uncertainly. The browser regression holds an actual model request pending, then releases it and checks one final reply. A separate induced browser-network failure verifies that Check outcome remains available.

The built-in arcade was also played through actual agent-browser mouse/keyboard input: 130 points earned, the natural sixty-second round completed, the save confirmation appeared, and the widget and reopened game retained 130. No score was written through the SDK or storage API. This is gameplay and reopen persistence for the built-in template, not arbitrary generated-app quality or a server-restart claim.

Agent-browser can display the nested sandbox but its current frame inspection cannot reliably reach the opaque plugin frame. The runner attaches the existing Playwright dependency to the same owned agent-browser browser before opening plugins, using visible frame controls and read-only DOM assertions. It preserves the original sandbox and normal host persistence. Reports identify this fallback. Synthetic MLB remains explicitly synthetic, including in optional real-model mode. Physical-device/WebKit, actual account consent, arbitrary app generation, distributed inference and the broader life vision remain separate dependencies. No existing preview, household service, Keychain, device permission or `~/.ellie` data was changed.

Final frozen-candidate validation used Node 24.21.0 and Bun 1.4.2. `bun run life:quality:browser` passed **5/5 scenarios**, zero failures/skips, with 137,483 ms in scenario execution. Reminder delivery took 65,887 ms and the natural arcade round/save/reopen took 62,800 ms; these are fixture workflow durations, not model latency. Browser console and page-error artifacts were empty, and all owned browser/fixture teardown completed. The report is retained in the private quality ledger, SHA-256 `1c634847f18123aebab15ff4361a0a0ba0b9fe1fb7ce00fc986642c928a3eca8`. The report records the parent revision and exact runner/fixture/built-asset digests; the adjacent frozen-source manifest binds the tested working changes to the final committed source.

The same pending scenario against the temporarily restored original banner condition failed at the intended recovery-element count (`1 !== 0`, exit one). The fixed source was restored and rebuilt before the successful full run. Final screenshots were reviewed for pending chat, the delivered notification, persisted arcade best and synthetic baseball standings.

`bun install --frozen-lockfile --ignore-scripts`, strict standalone TypeScript for both new scripts, the Life UI TypeScript project, focused Oxlint/format checks and `git diff --check` passed. `node --test tests/life-chat-progress.test.ts tests/life-quality-forgetting.test.ts` passed both cases. No duplicate full/native CI gate was run. The release coordinator integrated the original quality commit on the reviewed native/forgetting stack. A second complete agent-browser run on the combined source passed all five scenarios with no skips or cleanup error; frozen dependency installation, repository formatting and all TypeScript projects also passed. Hosted checks and merge remain separate from this local integration result. The running preview has not been updated with this fix.

## September 14 real-model checklist follow-through

An agent-browser trial against reviewed PR106 and a separately reserved local Qwen model reproduced two ordinary checklist requests returning prose while Plans stayed empty. The first request began with an introductory sentence, which also exposed an anchored host authorization check. Original prompts, empty-state snapshots, screenshots and outbound model requests remain in the private quality ledger.

The bounded fix shares a conservative direct-plan classifier between model validation and host authorization. It recognizes a direct imperative after a sentence boundary while withholding authority from supported quoted, reported, code, blockquote, negated and withdrawn forms. Existing title overlap and scope checks remain. A schema-valid prose-only answer to such a request enters the existing single repair under the original thirty-second deadline and inference lease. A genuine typed clarification or clear no-write refusal remains possible. A still-invalid repaired response fails before any proposed action runs.

Real-model iteration caught a second defect: the repair produced a flattened `create_plan` action instead of `life_operation` containing `intent.kind: create_plan`. Bounded wire capture confirmed the exact rejected output. The prompt now distinguishes those fields and supplies an unrelated complete envelope example whose content must come from the current request. The strict decoder still rejects the captured malformed action; no normalization, prose-to-plan shortcut or additional retry was added.

The final frozen candidate passed the opt-in `LIFE_QUALITY_SCENARIOS=planning` browser scenario with actual Qwen inference: empty Plans → ordinary compound request through the orb → one saved two-step plan → first checkbox completed → reload and reopen with the same plan and step IDs. The first step remained complete, the second unfinished, and the record revision increased to two. One submitted user turn used two outbound model calls, including the repair. The full scenario took **17,429 ms**, not a model-only latency measurement. Report SHA-256: `0586810884fda7ded99338f1466e39364983f43e85ed734d0a4b1c9388d6ce85`. The report and adjacent source manifest bind the tested code and built UI to the eventual commit. Browser console/page-error artifacts were empty, and owned fixture/browser teardown completed without error.

Exact focused Node validation passed **120/120**, zero failures or skips:

```sh
node --test tests/life-model.test.ts tests/life-model-latency.test.ts tests/life-model-scheduler.test.ts tests/life-harness.test.ts tests/life-plans.test.ts tests/life-planning-quality.test.ts
```

All application TypeScript projects, strict standalone TypeScript for both quality scripts, focused lint/format checks and `git diff --check` passed. A separate synthetic `LIFE_QUALITY_SCENARIOS=memory,pending-status` browser run passed **2/2 selected cases** with three unselected cases skipped, validating direct agent-browser completion polling and genuine interrupted-request recovery. The baseline five-case default remains unchanged; real-model planning requires explicit configuration and reservation.

Intermediate candidate failures are preserved separately: a mobile Plans-card center click overlapped the floating orb before any prompt was sent; a later run hit the browser tool's internal wait limit; and two runs exposed malformed repaired actions. The runner now uses ordinary scrolling plus fresh snapshots before clicking and bounded direct agent-browser status polling with immediate recovery-state evidence. The orb overlap remains a separate small-screen UX observation. This acceptance covers browser reload and reopen, not a process restart, physical-device behavior or broad model reliability. The live preview, household services, account data and native clients were not changed. The release coordinator owns final integration, hosted checks and merge.
